# Story 13.2: Add Employee Row Selection Checkboxes

## Status

**Pending**

---

## Story

**As a** HR Admin user,  
**I want** tiny checkboxes on the left side of each employee row (or in a logical position on mobile) that allow me to select employees,  
**so that** I can mark specific employees for bulk operations like export.

---

## Acceptance Criteria

**Given** I am viewing the employee table on desktop  
**When** I look at an employee row  
**Then** a small checkbox appears on the far left of the row  
**And** the checkbox is clearly visible but not intrusive  
**And** the checkbox is properly aligned with the row content

**Given** I am viewing the employee table on mobile (< 1024px width)  
**When** I look at an employee card/box  
**Then** a checkbox appears in a logical position (e.g., top-left corner of the card)  
**And** the checkbox is easily tappable (minimum 44x44px touch target)  
**And** the checkbox does not interfere with other card interactions

**Given** I see an employee row with an unchecked selection checkbox  
**When** I click the checkbox  
**Then** the checkbox becomes checked  
**And** the employee row receives a greyish tint background  
**And** the employee is marked as "selected" in the application state

**Given** I see an employee row with a checked selection checkbox  
**When** I click the checkbox again  
**Then** the checkbox becomes unchecked  
**And** the greyish tint is removed from the row  
**And** the employee is removed from the selected state

**Given** I have multiple employees selected  
**When** I view the table  
**Then** all selected employee rows show the greyish tint  
**And** all their checkboxes are checked  
**And** the selection state persists as I scroll or interact with the table

---

## Tasks / Subtasks

### Phase 1: Add Selection State Management (AC: All)

- [ ] **Task 1.1: Create Selection State**
  - [ ] Open `src/app/dashboard/page.tsx` or create selection context/store
  - [ ] Add state: `const [selectedEmployeeIds, setSelectedEmployeeIds] = useState<Set<string>>(new Set())`
  - [ ] Create helper functions: `toggleEmployeeSelection(id: string)`, `isEmployeeSelected(id: string)`
  - [ ] Consider using localStorage for session persistence if needed
  - [ ] Export selection state and handlers

- [ ] **Task 1.2: Add Selection Checkbox Column (Desktop)**
  - [ ] Open `src/components/dashboard/employee-table.tsx`
  - [ ] Add selection checkbox as first column in table definition
  - [ ] Use existing Checkbox component from `src/components/ui/checkbox.tsx`
  - [ ] Position checkbox on far left of row
  - [ ] Size checkbox appropriately: `w-4 h-4` or similar
  - [ ] Bind checkbox `checked` to `isEmployeeSelected(row.original.id)`
  - [ ] Bind checkbox `onCheckedChange` to `toggleEmployeeSelection(row.original.id)`

- [ ] **Task 1.3: Add Selection Checkbox to Mobile Cards**
  - [ ] Locate mobile card component (likely in same `employee-table.tsx` or separate component)
  - [ ] Add checkbox to card header or top-left area
  - [ ] Ensure checkbox has minimum 44x44px touch target for mobile
  - [ ] Position checkbox so it doesn't interfere with card interactions
  - [ ] Bind checkbox to same selection state as desktop

- [ ] **Task 1.4: Implement Greyish Tint for Selected Rows**
  - [ ] Update `TableRow` className logic in `employee-table.tsx`
  - [ ] Add conditional class: `isEmployeeSelected(row.original.id) ? 'bg-gray-100 dark:bg-gray-800' : ''`
  - [ ] Apply same tint to mobile cards when selected
  - [ ] Ensure tint is subtle but visible
  - [ ] Test tint works in both light and dark mode

- [ ] **Task 1.5: Ensure Selection Persists**
  - [ ] Verify selection state persists during scroll
  - [ ] Verify selection state persists during filter changes
  - [ ] Consider localStorage persistence for session (optional)
  - [ ] Test: Selection remains after table updates

---

## Dev Notes

### Selection State Management

Use React state to track selected employee IDs:
```typescript
const [selectedEmployeeIds, setSelectedEmployeeIds] = useState<Set<string>>(new Set());

const toggleEmployeeSelection = (id: string) => {
  setSelectedEmployeeIds(prev => {
    const next = new Set(prev);
    if (next.has(id)) {
      next.delete(id);
    } else {
      next.add(id);
    }
    return next;
  });
};

const isEmployeeSelected = (id: string) => selectedEmployeeIds.has(id);
```

### Visual Styling

Apply greyish tint using Tailwind classes:
- Desktop: `bg-gray-100 dark:bg-gray-800` on `TableRow`
- Mobile: Same classes on employee card component
- Ensure text remains readable with tinted background

### Checkbox Component

Use existing Checkbox component from `src/components/ui/checkbox.tsx`. Ensure proper ARIA labels for accessibility:
```tsx
<Checkbox
  checked={isEmployeeSelected(employee.id)}
  onCheckedChange={() => toggleEmployeeSelection(employee.id)}
  aria-label={`Select ${employee.first_name} ${employee.surname}`}
/>
```

### Source Tree References

- Employee table: `src/components/dashboard/employee-table.tsx`
- Checkbox component: `src/components/ui/checkbox.tsx`
- Dashboard page: `src/app/dashboard/page.tsx`

---

## Testing Requirements

**CRITICAL:** All tests in the entire project must pass before marking this story as "Ready for Review" or "Done".

**Test Organization:** All new tests must be placed in folders named for the epic and story number:
- Unit tests: `tests/unit/epic-13/story-13.2/`
- Integration tests: `tests/integration/epic-13/story-13.2/`
- E2E tests: `tests/e2e/epic-13/story-13.2/`
- This organization ensures that when multiple developers work on different stories, it's easy to identify which tests belong to which story

### Unit Tests

- [ ] **Test: Selection State Management**
  - [ ] Create test: `tests/unit/hooks/use-employee-selection.test.ts`
  - [ ] Test: `toggleEmployeeSelection` adds employee to selection
  - [ ] Test: `toggleEmployeeSelection` removes employee from selection
  - [ ] Test: `isEmployeeSelected` returns correct boolean
  - [ ] Test: Multiple employees can be selected simultaneously
  - [ ] Test: Selection state persists correctly

- [ ] **Test: Checkbox Component Integration**
  - [ ] Create test: `tests/unit/components/employee-selection-checkbox.test.tsx`
  - [ ] Test: Checkbox `checked` prop reflects selection state
  - [ ] Test: Clicking checkbox toggles selection
  - [ ] Test: Checkbox has proper ARIA labels

### Integration Tests

- [ ] **Test: Selection Visual Feedback**
  - [ ] Create test: `tests/integration/components/employee-selection-visual.test.tsx`
  - [ ] Test: Selected rows show greyish tint
  - [ ] Test: Unselected rows don't show tint
  - [ ] Test: Tint works in light and dark mode
  - [ ] Test: Mobile cards show tint when selected

### E2E Tests

- [ ] **Test: Employee Selection Workflow**
  - [ ] Create test: `tests/e2e/employee-selection.spec.ts`
  - [ ] Test: User can check checkbox to select employee
  - [ ] Test: User can uncheck checkbox to deselect employee
  - [ ] Test: Selected employees show greyish tint
  - [ ] Test: Multiple employees can be selected
  - [ ] Test: Selection persists during scroll
  - [ ] Test: Selection works on mobile view

### Manual Testing Checklist

- [ ] Checkbox appears on far left of desktop rows
- [ ] Checkbox appears in logical position on mobile cards
- [ ] Checkbox is easily clickable/tappable
- [ ] Clicking checkbox selects employee and shows greyish tint
- [ ] Clicking checked checkbox deselects employee and removes tint
- [ ] Multiple employees can be selected simultaneously
- [ ] Selection persists during scroll and filter changes
- [ ] Greyish tint is visible but doesn't obscure text
- [ ] Works correctly in light and dark mode

### Test Execution

Before marking story as complete:
1. Run all unit tests: `pnpm test`
2. Run all integration tests: `pnpm test:integration`
3. Run all E2E tests: `pnpm test:e2e`
4. Verify ALL tests in the project pass (not just tests for this story)
5. Fix any failing tests before proceeding

---

## Prerequisites

- Story 2.1 (Employee List Table View) - Employee table component must exist

