# Story 13.7: Write Comprehensive Export Tests

## Status

**Pending**

---

## Story

**As a** developer,  
**I want** comprehensive test coverage for all export functionality,  
**so that** future changes don't break exports and I can confidently refactor export code.

---

## Acceptance Criteria

**Given** I run the test suite  
**When** export tests execute  
**Then** tests cover: export with selected employees, export with field selection, export with no selection, export crew ready, export error handling  
**And** all export tests pass

**Given** I modify export functionality  
**When** I run the export tests  
**Then** tests immediately reveal if my changes broke existing export behavior  
**And** I can fix issues before deployment

---

## Tasks / Subtasks

### Phase 1: Unit Tests for Export Services (AC: All)

- [ ] **Task 1.1: Test Export Service Functions**
  - [ ] Create test: `tests/unit/services/export-service.test.ts`
  - [ ] Test: Export filters by selected employee IDs
  - [ ] Test: Export includes only selected fields
  - [ ] Test: Export handles empty selection gracefully
  - [ ] Test: Export handles invalid field names
  - [ ] Test: CSV generation produces correct format
  - [ ] Test: Field ordering is maintained

- [ ] **Task 1.2: Test Export Crew Ready Service**
  - [ ] Create test: `tests/unit/services/export-crew-ready.test.ts`
  - [ ] Test: Export crew ready filters by selected IDs
  - [ ] Test: Export crew ready only marks selected employees as done
  - [ ] Test: Export crew ready handles empty selection
  - [ ] Test: Export crew ready validates crew ready criteria

### Phase 2: Integration Tests for Export API Endpoints (AC: All)

- [ ] **Task 2.1: Test General Export API**
  - [ ] Create test: `tests/integration/api/export-selected.test.ts`
  - [ ] Test: POST /api/employees/export with selected IDs
  - [ ] Test: POST /api/employees/export with field selection
  - [ ] Test: POST /api/employees/export with empty selection (400 error)
  - [ ] Test: POST /api/employees/export with invalid IDs
  - [ ] Test: CSV response has correct headers
  - [ ] Test: CSV response contains correct data

- [ ] **Task 2.2: Test Export Crew Ready API**
  - [ ] Create test: `tests/integration/api/export-crew-ready-selected.test.ts`
  - [ ] Test: POST /api/employees/export-crew-ready with selected IDs
  - [ ] Test: Only selected employees are exported
  - [ ] Test: Only selected employees are marked as done
  - [ ] Test: Empty selection returns 400 error
  - [ ] Test: CSV contains correct employee data

- [ ] **Task 2.3: Test Export Error Handling**
  - [ ] Test: API handles database errors gracefully
  - [ ] Test: API handles invalid request body
  - [ ] Test: API handles missing required fields
  - [ ] Test: API returns appropriate error messages

### Phase 3: E2E Tests for Export Workflows (AC: All)

- [ ] **Task 3.1: Test General Export Workflow**
  - [ ] Create test: `tests/e2e/export-workflow.spec.ts`
  - [ ] Test: User selects employees and exports (CSV contains only selected)
  - [ ] Test: User selects fields and exports (CSV contains only selected fields)
  - [ ] Test: User exports with no selection (error message)
  - [ ] Test: CSV file downloads correctly
  - [ ] Test: CSV file has correct filename

- [ ] **Task 3.2: Test Export Crew Ready Workflow**
  - [ ] Test: User selects employees and exports crew ready
  - [ ] Test: Only selected employees are exported
  - [ ] Test: Only selected employees are marked as done
  - [ ] Test: Export crew ready with no selection (error message)
  - [ ] Test: CSV contains crew ready indicator

- [ ] **Task 3.3: Test Export Field Selection Workflow**
  - [ ] Test: User opens export dialog
  - [ ] Test: Visible columns are pre-selected
  - [ ] Test: User can select/deselect fields
  - [ ] Test: Export generates CSV with selected fields
  - [ ] Test: CSV headers match selected fields

### Phase 4: Test CSV Format Validation (AC: All)

- [ ] **Task 4.1: Test CSV Structure**
  - [ ] Test: CSV has correct headers
  - [ ] Test: CSV has correct number of rows
  - [ ] Test: CSV data matches employee data
  - [ ] Test: CSV handles special characters correctly
  - [ ] Test: CSV handles null/empty values correctly

- [ ] **Task 4.2: Test CSV Encoding**
  - [ ] Test: CSV uses correct encoding (UTF-8)
  - [ ] Test: CSV handles Swedish characters (å, ä, ö)
  - [ ] Test: CSV handles special characters in names
  - [ ] Test: CSV is properly formatted for Excel

### Phase 5: Test Edge Cases (AC: All)

- [ ] **Task 5.1: Test Edge Cases**
  - [ ] Test: Export with single employee
  - [ ] Test: Export with all employees
  - [ ] Test: Export with employees from multiple pages
  - [ ] Test: Export with employees that have null values
  - [ ] Test: Export with employees that have special characters
  - [ ] Test: Export with very large selection (performance)

---

## Dev Notes

### Test File Structure

Follow existing test patterns from `tests/integration/api/export-workflow.test.ts`:
- Use same test setup and teardown
- Use same test data fixtures
- Use same assertion patterns
- Use same mocking strategies

### Test Coverage Goals

- Unit tests: Cover all export service functions
- Integration tests: Cover all export API endpoints
- E2E tests: Cover all export user workflows
- Edge cases: Cover error scenarios and boundary conditions

### Test Data

Create test fixtures for:
- Employees with various data states
- Selected employee IDs
- Field selections
- Expected CSV output

### Source Tree References

- Existing export tests: `tests/integration/api/export-workflow.test.ts`
- Export service: `src/lib/services/export-service.ts`
- Export APIs: `src/app/api/employees/export/route.ts`, `src/app/api/employees/export-crew-ready/route.ts`

---

## Testing Requirements

**CRITICAL:** All tests in the entire project must pass before marking this story as "Ready for Review" or "Done".

**Test Organization:** All new tests must be placed in folders named for the epic and story number:
- Unit tests: `tests/unit/epic-13/story-13.7/`
- Integration tests: `tests/integration/epic-13/story-13.7/`
- E2E tests: `tests/e2e/epic-13/story-13.7/`
- This organization ensures that when multiple developers work on different stories, it's easy to identify which tests belong to which story

### Unit Tests

- [ ] **Test: Export Service Functions**
  - [ ] File: `tests/unit/services/export-service.test.ts`
  - [ ] Coverage: All export service functions
  - [ ] Test cases: Selected IDs, field selection, error handling
  - [ ] All tests pass

- [ ] **Test: Export Crew Ready Service**
  - [ ] File: `tests/unit/services/export-crew-ready.test.ts`
  - [ ] Coverage: Crew ready export logic
  - [ ] Test cases: Selected IDs, crew ready criteria, marking as done
  - [ ] All tests pass

### Integration Tests

- [ ] **Test: General Export API**
  - [ ] File: `tests/integration/api/export-selected.test.ts`
  - [ ] Coverage: POST /api/employees/export endpoint
  - [ ] Test cases: Selected IDs, field selection, error handling, CSV format
  - [ ] All tests pass

- [ ] **Test: Export Crew Ready API**
  - [ ] File: `tests/integration/api/export-crew-ready-selected.test.ts`
  - [ ] Coverage: POST /api/employees/export-crew-ready endpoint
  - [ ] Test cases: Selected IDs, marking as done, error handling
  - [ ] All tests pass

### E2E Tests

- [ ] **Test: Export Workflows**
  - [ ] File: `tests/e2e/export-workflow.spec.ts`
  - [ ] Coverage: All export user workflows
  - [ ] Test cases: General export, crew ready export, field selection, error scenarios
  - [ ] All tests pass

### Test Execution

Before marking story as complete:
1. Run all unit tests: `pnpm test`
2. Run all integration tests: `pnpm test:integration`
3. Run all E2E tests: `pnpm test:e2e`
4. Verify ALL tests in the project pass (not just tests for this story)
5. Fix any failing tests before proceeding
6. Verify test coverage meets requirements
7. Document test coverage metrics

### Test Coverage Metrics

- Unit test coverage: >80% for export services
- Integration test coverage: All export API endpoints
- E2E test coverage: All export user workflows
- Edge case coverage: Error scenarios and boundary conditions

---

## Prerequisites

- Story 13.4 (Export Only Selected Employees) - Export functionality must exist
- Story 13.6 (General Export Button with Field Selection) - Field selection must exist

