# Story 6.6: Column Management UX Improvements

## Status

**Ready for Development**

---

## Story

**As an** HR Admin,  
**I want** clear visual feedback for hidden columns and the ability to reorder columns via drag-and-drop,  
**so that** I can customize the table layout for my workflow.

---

## Acceptance Criteria

1. **Add Column Button:**
   - "Add Column" button visible on Column Settings page for HR Admin
   - Opens modal with fields: Column Name, Data Type (Text/Number/Date/Boolean), Category (optional)
   - Clicking "Create" adds column to column_config table and makes it visible in employee table

2. **Hide Column Visual Feedback:**
   - Hidden columns show "Hidden" badge in Column Settings table
   - Visible columns show "Visible" badge or checkmark icon
   - Toggle button changes text: "Hide" ⇄ "Show" based on current state
   - Hidden columns do NOT appear in employee table (already implemented, just need visual indicator)

3. **Drag-and-Drop Column Reordering:**
   - Drag handle icon (☰) on each column row in Column Settings table
   - HR Admin can drag columns to reorder them
   - New order saved to `column_config.display_order` field (new database column)
   - Employee table columns render in order specified by `display_order`
   - Reordering is persistent across sessions

4. **Database Schema Update:**
   - Add `display_order` column to `column_config` table (integer, default 0)
   - Migration script to populate default order for existing columns

5. **Responsive Design:**
   - Drag-and-drop works on desktop (mouse)
   - Mobile: Show "Move Up" / "Move Down" buttons instead of drag-and-drop

6. **Translations:**
   - "Add Column" button translated
   - "Hidden" / "Visible" badges translated
   - Drag handle accessible label: "Reorder column" (English) / "Ordna kolumn" (Swedish)

---

## ⚠️ CRITICAL ROUTING/AUTH SAFEGUARDS ⚠️

**LESSON LEARNED FROM STORY 6.6 FAILURE (November 2, 2025):**

Original Story 6.6 implementation introduced critical authentication and routing bugs that cascaded into Stories 6.7-6.9, requiring full codebase revert to Story 6.5. This section documents **MANDATORY** safeguards to prevent recurrence.

### 🚨 DO NOT MODIFY These Files (Unless Absolutely Required)

**High-Risk Authentication/Routing Files:**
- `middleware.ts` - **FROZEN** unless authentication logic change required
- `src/app/page.tsx` - **FROZEN** root page routing
- `src/app/[locale]/page.tsx` - **FROZEN** localized root routing
- `src/app/[locale]/(auth)/login/page.tsx` - **FROZEN** login page routing
- `src/app/[locale]/(auth)/login/login-form.tsx` - **FROZEN** login form logic

**If You MUST Modify These Files:**
1. Create separate story dedicated to routing/auth changes
2. Get explicit approval from PO (Sarah) before proceeding
3. Write comprehensive E2E tests for ALL auth flows BEFORE making changes
4. Test manually with:
   - Fresh login (cleared cookies)
   - Second visit (existing cookies)
   - Page refresh after login
   - Direct URL navigation to protected routes
   - Logout → login cycle
5. Document EXACT reason for modification in commit message

### ✅ Authentication Best Practices (From Post-Mortem Analysis)

**Supabase SSR Authentication Rules:**

1. **ALWAYS use `getUser()` for auth checks** (validates JWT server-side)
   - ❌ NEVER use `getSession()` in middleware or pages (only reads cookies, no validation)
   - ✅ Correct: `const { data: { user } } = await supabase.auth.getUser();`
   - ❌ Wrong: `const { data: { session } } = await supabase.auth.getSession();`

2. **Use `window.location.href` for redirects after login** (forces full page reload)
   - ❌ NEVER use `router.push()` after authentication state change (client-side navigation doesn't sync cookies)
   - ✅ Correct: `window.location.href = "/dashboard";`
   - ❌ Wrong: `router.push("/dashboard");`

3. **Server components for auth-dependent pages** (no client-side auth checks)
   - ✅ Root pages (`page.tsx`) should be async server components
   - ✅ Auth check happens on server before rendering
   - ❌ Don't use client components with `useEffect` auth checks (causes flashing)

4. **Consistent auth checks across all entry points**
   - Middleware checks auth for route protection
   - Page components check auth for redirects
   - Both must use `getUser()` (not `getSession()`)

**Testing Checklist Before ANY Auth/Routing Change:**
```markdown
- [ ] Clear all browser cookies
- [ ] Navigate to / → Should redirect to /sv or show Swedish root page
- [ ] Click login button → Should show login form
- [ ] Enter valid credentials → Should redirect to /sv/dashboard with FULL PAGE RELOAD
- [ ] Navigate to / again → Should immediately redirect to /sv/dashboard (no login button shown)
- [ ] Refresh page → Should stay on dashboard (no redirect loop)
- [ ] Open new tab, go to / → Should redirect to dashboard (session persists)
- [ ] Logout → Should redirect to /sv
- [ ] Try accessing /sv/dashboard directly → Should redirect to login
- [ ] Login again → Should work (no "second visit" bugs)
````

### 🔒 Safe Development Boundaries for Story 6.6

**This Story CAN Safely Modify:**
- `src/app/[locale]/(protected)/column-settings/page.tsx` - Column settings UI (protected route)
- `src/components/dashboard/column-settings-table.tsx` - Column table component (new component likely needed)
- `src/app/api/column-config/*` - API routes for column CRUD operations
- `src/lib/validation/column-schema.ts` - Column validation schemas
- Database migrations for `column_config` table schema changes
- Translation files (`messages/en.json`, `messages/sv.json`)

**This Story CANNOT Modify:**
- Any file in `src/app/(auth)/` directory
- Any file in root `src/app/` (page.tsx, layout.tsx)
- `middleware.ts`
- Any file containing Supabase `createClient` or auth logic

**This Story Should NOT Create:**
- New route groups that affect authentication middleware
- New server actions that modify user session
- New middleware or middleware configuration changes

---

## Tasks / Subtasks

### Phase 0: Pre-Implementation Safety Checks (MANDATORY)

- [ ] **Task 0.1: Verify Working Auth State**
  - [ ] Run dev server: `pnpm dev`
  - [ ] Complete full authentication testing checklist (see above)
  - [ ] Document current working state: "Auth works correctly as of [date/commit]"
  - [ ] Take screenshot of successful login flow
  - [ ] Commit hash of working state: `___________`

- [ ] **Task 0.2: Review Files to Modify**
  - [ ] List all files this story will modify (AC 1-6)
  - [ ] Cross-reference with "DO NOT MODIFY" list above
  - [ ] If overlap exists: STOP and consult PO
  - [ ] Document approved file list in Dev Notes

- [ ] **Task 0.3: Create Isolated Feature Branch**
  - [ ] Branch from `main` at working Story 6.5 commit
  - [ ] Branch name: `feature/story-6.6-column-ux`
  - [ ] Verify branch is clean: `git status` should show no changes
  - [ ] Document branch: `git log --oneline -1` → Copy commit hash

### Phase 1: Database Migration (AC: 4)

- [ ] **Task 1.1: Create Migration File**
  - [ ] Create `migrations/[timestamp]_add_display_order_to_column_config.sql`
  - [ ] Add `display_order INTEGER DEFAULT 0` column to `column_config` table
  - [ ] Add SQL to populate default order for existing columns (ROW_NUMBER() or sequential)
  - [ ] Test migration on local database
  - [ ] Verify existing columns have sequential display_order values

- [ ] **Task 1.2: Update Database Schema Documentation**
  - [ ] Update `docs/architecture/database-schema.md`
  - [ ] Document `column_config.display_order` field
  - [ ] Add note: "display_order determines column render order in employee table"

- [ ] **Task 1.3: Apply Migration Locally**
  - [ ] Run migration: `pnpm supabase migration up`
  - [ ] Verify column added: Query `column_config` table
  - [ ] Check default values populated correctly
  - [ ] Test rollback if needed

### Phase 2: Add Column Functionality (AC: 1)

- [ ] **Task 2.1: Create Column Schema**
  - [ ] Create or update `src/lib/validation/column-schema.ts`
  - [ ] Define `AddColumnFormData` interface
  - [ ] Create Zod schema: `addColumnSchema`
  - [ ] Fields: column_name (string, required), data_type (enum), category (string, optional)
  - [ ] Validation: column_name unique, max 100 chars

- [ ] **Task 2.2: Create Add Column API Route**
  - [ ] Create `src/app/api/column-config/route.ts` (POST)
  - [ ] Require HR Admin authentication
  - [ ] Validate request body with `addColumnSchema`
  - [ ] Insert into `column_config` table
  - [ ] Set `is_visible = true` and `display_order = MAX(display_order) + 1`
  - [ ] Return new column data

- [ ] **Task 2.3: Create Add Column Modal Component**
  - [ ] Create `src/components/dashboard/add-column-modal.tsx`
  - [ ] Form fields: Column Name, Data Type (dropdown), Category (optional)
  - [ ] Data Type options: Text, Number, Date, Boolean
  - [ ] Submit button calls POST /api/column-config
  - [ ] Success: Close modal, refresh column list
  - [ ] Error: Display validation errors

- [ ] **Task 2.4: Add "Add Column" Button to Column Settings Page**
  - [ ] Open `src/app/[locale]/(protected)/column-settings/page.tsx`
  - [ ] Add button in header: "Add Column" (HR Admin only)
  - [ ] Button opens `AddColumnModal`
  - [ ] Translate button text in `messages/en.json` and `messages/sv.json`

- [ ] **Task 2.5: Test Add Column Flow**
  - [ ] Manual test: Click "Add Column" button
  - [ ] Fill form with valid data
  - [ ] Submit and verify new column appears in table
  - [ ] Verify new column has highest `display_order` value
  - [ ] Verify new column is visible in employee table

### Phase 3: Hide/Show Column Visual Feedback (AC: 2)

- [ ] **Task 3.1: Add Visibility Badge Component**
  - [ ] Create `src/components/ui/visibility-badge.tsx` (if not exists)
  - [ ] Component props: `isVisible: boolean`
  - [ ] Render: "Visible" badge (green) or "Hidden" badge (gray)
  - [ ] Use existing Badge component from shadcn/ui
  - [ ] Translate badge text

- [ ] **Task 3.2: Update Column Settings Table**
  - [ ] Open existing column settings table component (or create if needed)
  - [ ] Add "Visibility" column showing badge
  - [ ] Update "Hide/Show" button text based on current visibility state
  - [ ] Button text: "Hide" when visible, "Show" when hidden
  - [ ] Translate button text

- [ ] **Task 3.3: Update Toggle Visibility API Route**
  - [ ] Verify existing PATCH `/api/column-config/[id]/toggle-visibility` route exists
  - [ ] If not, create it
  - [ ] Update `is_visible` field in database
  - [ ] Return updated column data

- [ ] **Task 3.4: Test Visibility Toggle**
  - [ ] Manual test: Click "Hide" button on visible column
  - [ ] Verify badge changes to "Hidden"
  - [ ] Verify button text changes to "Show"
  - [ ] Verify column disappears from employee table
  - [ ] Click "Show" and verify reverse behavior

### Phase 4: Drag-and-Drop Column Reordering (AC: 3, 5)

- [ ] **Task 4.1: Install Drag-and-Drop Library**
  - [ ] Install `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`
  - [ ] Run: `pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`
  - [ ] Verify package versions compatible with React 18+

- [ ] **Task 4.2: Create Draggable Column Row Component**
  - [ ] Create `src/components/dashboard/draggable-column-row.tsx`
  - [ ] Use `useSortable` hook from dnd-kit
  - [ ] Add drag handle icon (☰) with accessible label
  - [ ] Apply drag preview styling
  - [ ] Pass column data as props

- [ ] **Task 4.3: Implement Drag Context in Column Settings Table**
  - [ ] Update column settings table to use `DndContext`
  - [ ] Wrap table rows in `SortableContext`
  - [ ] Handle `onDragEnd` event
  - [ ] Calculate new display_order values for all columns
  - [ ] Call API to batch update display_order

- [ ] **Task 4.4: Create Reorder API Route**
  - [ ] Create `src/app/api/column-config/reorder/route.ts` (PATCH)
  - [ ] Accept array of `{ id, display_order }` objects
  - [ ] Validate HR Admin role
  - [ ] Batch update all columns in single transaction
  - [ ] Return success status

- [ ] **Task 4.5: Add Mobile Reorder Buttons (Responsive)**
  - [ ] Detect mobile viewport (useMediaQuery or Tailwind breakpoints)
  - [ ] On mobile: Hide drag handles, show "Move Up" / "Move Down" buttons
  - [ ] Buttons call same reorder API with adjusted display_order
  - [ ] Translate button text

- [ ] **Task 4.6: Update Employee Table to Respect display_order**
  - [ ] Open employee table component
  - [ ] Modify column fetch query to ORDER BY display_order ASC
  - [ ] Verify columns render in correct order
  - [ ] Test with reordered columns

- [ ] **Task 4.7: Test Drag-and-Drop Reordering**
  - [ ] Manual test: Drag column row to new position
  - [ ] Verify visual feedback during drag
  - [ ] Verify column order persists after page refresh
  - [ ] Verify employee table columns reorder correctly
  - [ ] Test on mobile: Verify "Move Up/Down" buttons work

### Phase 5: Translations (AC: 6)

- [ ] **Task 5.1: Add English Translations**
  - [ ] Open `messages/en.json`
  - [ ] Add keys:
    - `columnSettings.addColumn`: "Add Column"
    - `columnSettings.visibility.visible`: "Visible"
    - `columnSettings.visibility.hidden`: "Hidden"
    - `columnSettings.actions.hide`: "Hide"
    - `columnSettings.actions.show`: "Show"
    - `columnSettings.dragHandle.aria`: "Reorder column"
    - `columnSettings.mobileReorder.up`: "Move Up"
    - `columnSettings.mobileReorder.down`: "Move Down"
    - `columnSettings.addColumnModal.title`: "Add New Column"
    - `columnSettings.addColumnModal.columnName`: "Column Name"
    - `columnSettings.addColumnModal.dataType`: "Data Type"
    - `columnSettings.addColumnModal.category`: "Category (Optional)"
    - `columnSettings.addColumnModal.create`: "Create Column"

- [ ] **Task 5.2: Add Swedish Translations**
  - [ ] Open `messages/sv.json`
  - [ ] Add corresponding Swedish translations:
    - `columnSettings.addColumn`: "Lägg till kolumn"
    - `columnSettings.visibility.visible`: "Synlig"
    - `columnSettings.visibility.hidden`: "Dold"
    - `columnSettings.actions.hide`: "Dölj"
    - `columnSettings.actions.show`: "Visa"
    - `columnSettings.dragHandle.aria`: "Ordna kolumn"
    - `columnSettings.mobileReorder.up`: "Flytta upp"
    - `columnSettings.mobileReorder.down`: "Flytta ner"
    - `columnSettings.addColumnModal.title`: "Lägg till ny kolumn"
    - `columnSettings.addColumnModal.columnName`: "Kolumnnamn"
    - `columnSettings.addColumnModal.dataType`: "Datatyp"
    - `columnSettings.addColumnModal.category`: "Kategori (Valfritt)"
    - `columnSettings.addColumnModal.create`: "Skapa kolumn"

- [ ] **Task 5.3: Verify Translation Usage**
  - [ ] Use `useTranslations('columnSettings')` in all components
  - [ ] Test language switching between English and Swedish
  - [ ] Verify all labels display correctly in both languages

### Phase 6: Integration Testing (All ACs)

- [ ] **Task 6.1: Write Unit Tests**
  - [ ] Create `tests/unit/validation/column-schema.test.ts`
  - [ ] Test: Valid column name accepts
  - [ ] Test: Empty column name rejects
  - [ ] Test: Column name > 100 chars rejects
  - [ ] Test: Valid data types accept (Text, Number, Date, Boolean)
  - [ ] Test: Invalid data type rejects

- [ ] **Task 6.2: Write API Integration Tests**
  - [ ] Create `tests/integration/api/column-config-add.test.ts`
  - [ ] Test: POST /api/column-config creates column with correct display_order
  - [ ] Test: POST /api/column-config requires HR Admin role
  - [ ] Test: Duplicate column name rejects
  - [ ] Create `tests/integration/api/column-config-reorder.test.ts`
  - [ ] Test: PATCH /api/column-config/reorder updates all display_order values
  - [ ] Test: Reorder persists across sessions

- [ ] **Task 6.3: Write Component Tests**
  - [ ] Create `tests/unit/components/add-column-modal.test.tsx`
  - [ ] Test: Modal opens and closes correctly
  - [ ] Test: Form validation prevents empty column name
  - [ ] Test: Successful submission calls API and closes modal

- [ ] **Task 6.4: Manual End-to-End Testing**
  - [ ] Complete full workflow test:
    1. Login as HR Admin
    2. Navigate to Column Settings
    3. Click "Add Column" → Fill form → Submit
    4. Verify new column appears
    5. Drag column to new position
    6. Verify employee table reflects new order
    7. Hide column → Verify badge changes
    8. Refresh page → Verify order persists
    9. Show column → Verify badge changes
    10. Logout and login → Verify settings persist

### Phase 7: Authentication Regression Testing (MANDATORY)

- [ ] **Task 7.1: Complete Full Auth Testing Checklist**
  - [ ] Clear all browser cookies
  - [ ] Test complete auth flow (see "Testing Checklist" above)
  - [ ] Document test results: "All auth flows working: [date]"
  - [ ] If ANY test fails: STOP, debug, do NOT proceed

- [ ] **Task 7.2: Verify No Auth Files Modified**
  - [ ] Run: `git diff main --name-only`
  - [ ] Cross-reference with "DO NOT MODIFY" list
  - [ ] If auth file modified: Review change, document reason
  - [ ] Get PO approval before merging

- [ ] **Task 7.3: Run All Existing Tests**
  - [ ] Run: `pnpm test`
  - [ ] Verify 100% of existing tests still pass
  - [ ] If any test fails: Identify regression, fix before proceeding

### Phase 8: Documentation and Cleanup

- [ ] **Task 8.1: Update User Guide**
  - [ ] Open `docs/USER_GUIDE.md`
  - [ ] Add section: "Customizing Column Layout"
  - [ ] Document: How to reorder columns, hide/show columns, add custom columns
  - [ ] Include screenshots or step-by-step instructions

- [ ] **Task 8.2: Update API Documentation**
  - [ ] Open `docs/API_DOCUMENTATION.md`
  - [ ] Document new endpoints:
    - POST /api/column-config (Create column)
    - PATCH /api/column-config/reorder (Reorder columns)
    - PATCH /api/column-config/[id]/toggle-visibility (Toggle visibility)

- [ ] **Task 8.3: Code Review Checklist**
  - [ ] All TypeScript types properly defined
  - [ ] No `any` types used
  - [ ] Error handling comprehensive
  - [ ] Loading states implemented for all async operations
  - [ ] Accessibility: Keyboard navigation for drag-and-drop
  - [ ] Mobile responsive design tested

---

## Dev Notes

### Purpose

This story enhances the Column Settings page with professional UX features: visual feedback for column visibility, drag-and-drop reordering, and ability to add custom columns. These improvements enable HR Admins to customize the employee table layout for their specific workflow needs.

**Source:** Epic 6, Story 6.6 - Employee Form & Data Management Enhancements  
**Location:** `docs/prd/epic-6-employee-form-data-management-enhancements.md`

### Critical Context: Why This Story Failed Previously

**Incident Date:** November 2, 2025  
**Root Cause:** Story 6.6 implementation modified authentication-critical files (`middleware.ts`, root pages) without comprehensive testing, introducing redirect loops and login failures.

**Cascade Failures:**
- Story 6.7: Attempted to fix 6.6 issues, made things worse
- Story 6.8: UI cleanup story inadvertently modified root page auth logic
- Story 6.9: Created to fix redirect loops, identified THREE separate auth bugs
- **Result:** Full revert to Story 6.5 required, 4 stories worth of work discarded

**Key Lessons:**
1. ✅ Column management is LOW RISK (isolated to protected routes)
2. ❌ ANY modification to auth/routing files is EXTREMELY HIGH RISK
3. ✅ Comprehensive testing BEFORE code changes prevents cascading failures
4. ❌ "Quick fixes" to auth issues without root cause analysis create more bugs
5. ✅ Supabase SSR has specific patterns that MUST be followed exactly

**Safeguards in This Story:**
- Phase 0: Pre-implementation safety checks (NEW)
- Phase 7: Authentication regression testing (NEW)
- Explicit "DO NOT MODIFY" file list
- Mandatory auth testing checklist before merge
- PO approval required for any auth file changes

### Previous Story Insights

**From Story 6.5 (Relax CSV Import Validation):**

Story 6.5 was the LAST working story before regression. It successfully:
- Updated validation schemas without touching auth logic
- Modified API routes in isolated, protected endpoints
- Comprehensive testing with 28 passing tests
- No auth/routing files modified
- **Status:** PASS, 90/100 quality score

**Key Learning:** Story 6.5 demonstrates correct scope isolation. Story 6.6 should follow same pattern: modify only protected route components and API routes.

[Source: Story 6.5 Dev Notes, QA Results]

**From Story 2.4 (Column Visibility Management):**

Story 2.4 implemented basic column hide/show functionality:
- `column_config` table stores `is_visible` boolean
- Employee table filters columns WHERE is_visible = true
- Toggle API endpoint: PATCH `/api/column-config/[id]/toggle-visibility`
- **Location:** `src/app/api/column-config/[id]/toggle-visibility/route.ts` (verify exists)

**Existing Implementation to Build On:**
- Column configuration UI likely exists in Column Settings page
- May already have table displaying columns with toggle buttons
- This story ADDS: Visual badges, drag-and-drop, add column functionality

[Source: Epic 2 Stories, Column Config Implementation]

### Data Models

[Source: docs/architecture/data-models.md#column-config]

**Current column_config Schema:**

```sql
CREATE TABLE column_config (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  column_name TEXT NOT NULL UNIQUE,
  data_type TEXT NOT NULL, -- 'text', 'number', 'date', 'boolean'
  category TEXT,
  is_visible BOOLEAN DEFAULT true,
  is_system BOOLEAN DEFAULT false, -- System columns cannot be deleted
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

**Migration to Add (Task 1.1):**

```sql
-- Add display_order column
ALTER TABLE column_config
ADD COLUMN display_order INTEGER DEFAULT 0;

-- Populate default order for existing columns
UPDATE column_config
SET display_order = row_number
FROM (
  SELECT id, ROW_NUMBER() OVER (ORDER BY created_at) AS row_number
  FROM column_config
) AS ordered_columns
WHERE column_config.id = ordered_columns.id;
```

**Updated TypeScript Interface:**

```typescript
export interface ColumnConfig {
  id: string; // UUID
  column_name: string; // Unique
  data_type: 'text' | 'number' | 'date' | 'boolean';
  category: string | null; // Optional grouping
  is_visible: boolean; // Hide/show column
  is_system: boolean; // Prevent deletion of core columns
  display_order: number; // NEW - determines render order
  created_at: string; // ISO timestamp
  updated_at: string; // ISO timestamp
}
```

### File Locations

[Source: docs/architecture/unified-project-structure.md]

**Files to Modify (SAFE - No Auth Risk):**

- `migrations/[timestamp]_add_display_order_to_column_config.sql` - NEW
- `src/lib/validation/column-schema.ts` - NEW (or update existing)
- `src/app/api/column-config/route.ts` - NEW (POST for add column)
- `src/app/api/column-config/reorder/route.ts` - NEW (PATCH for reorder)
- `src/app/[locale]/(protected)/column-settings/page.tsx` - UPDATE (add button, drag-and-drop)
- `src/components/dashboard/add-column-modal.tsx` - NEW
- `src/components/dashboard/draggable-column-row.tsx` - NEW
- `src/components/ui/visibility-badge.tsx` - NEW
- `messages/en.json` - UPDATE (translations)
- `messages/sv.json` - UPDATE (translations)
- `docs/USER_GUIDE.md` - UPDATE (documentation)
- `docs/API_DOCUMENTATION.md` - UPDATE (documentation)
- `docs/architecture/database-schema.md` - UPDATE (schema docs)

**Files to VERIFY EXIST (from Story 2.4):**

- `src/app/api/column-config/[id]/toggle-visibility/route.ts` - Visibility toggle endpoint
- `src/app/[locale]/(protected)/column-settings/page.tsx` - Column settings UI page

**Files to CREATE for Testing:**

- `tests/unit/validation/column-schema.test.ts`
- `tests/integration/api/column-config-add.test.ts`
- `tests/integration/api/column-config-reorder.test.ts`
- `tests/unit/components/add-column-modal.test.tsx`

**Files FORBIDDEN to Modify:**

- `middleware.ts` - ❌ HIGH RISK
- `src/app/page.tsx` - ❌ HIGH RISK
- `src/app/[locale]/page.tsx` - ❌ HIGH RISK
- `src/app/[locale]/(auth)/**/*` - ❌ HIGH RISK
- Any file containing `createClient` or Supabase auth logic - ❌ HIGH RISK

### Drag-and-Drop Implementation Pattern

[Source: dnd-kit documentation, React best practices]

**Library Choice: @dnd-kit**
- Modern, accessible, performant
- Touch-friendly for mobile
- TypeScript support
- Active maintenance
- React 18+ compatible

**Basic Implementation Pattern:**

```tsx
import {
  DndContext,
  closestCenter,
  KeyboardSensor,
  PointerSensor,
  useSensor,
  useSensors,
} from '@dnd-kit/core';
import {
  arrayMove,
  SortableContext,
  sortableKeyboardCoordinates,
  verticalListSortingStrategy,
  useSortable,
} from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

// Sortable row component
function DraggableColumnRow({ column }) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id: column.id });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
  };

  return (
    <tr ref={setNodeRef} style={style}>
      <td>
        <button {...attributes} {...listeners} aria-label="Reorder column">
          ☰
        </button>
      </td>
      <td>{column.column_name}</td>
      {/* Other cells */}
    </tr>
  );
}

// Parent table component
function ColumnSettingsTable({ columns }) {
  const [items, setItems] = useState(columns);
  const sensors = useSensors(
    useSensor(PointerSensor),
    useSensor(KeyboardSensor, {
      coordinateGetter: sortableKeyboardCoordinates,
    })
  );

  function handleDragEnd(event) {
    const { active, over } = event;
    if (active.id !== over.id) {
      setItems((items) => {
        const oldIndex = items.findIndex((item) => item.id === active.id);
        const newIndex = items.findIndex((item) => item.id === over.id);
        const newItems = arrayMove(items, oldIndex, newIndex);

        // Update display_order based on new array position
        const updates = newItems.map((item, index) => ({
          id: item.id,
          display_order: index,
        }));

        // Call API to persist changes
        fetch('/api/column-config/reorder', {
          method: 'PATCH',
          body: JSON.stringify({ columns: updates }),
        });

        return newItems;
      });
    }
  }

  return (
    <DndContext
      sensors={sensors}
      collisionDetection={closestCenter}
      onDragEnd={handleDragEnd}
    >
      <SortableContext items={items} strategy={verticalListSortingStrategy}>
        <table>
          <thead>{/* headers */}</thead>
          <tbody>
            {items.map((column) => (
              <DraggableColumnRow key={column.id} column={column} />
            ))}
          </tbody>
        </table>
      </SortableContext>
    </DndContext>
  );
}
```

**Mobile Responsive Pattern:**

```tsx
function ColumnRow({ column, onMoveUp, onMoveDown, isMobile }) {
  if (isMobile) {
    return (
      <tr>
        <td>{column.column_name}</td>
        <td>
          <button onClick={() => onMoveUp(column.id)}>
            {t('mobileReorder.up')}
          </button>
          <button onClick={() => onMoveDown(column.id)}>
            {t('mobileReorder.down')}
          </button>
        </td>
      </tr>
    );
  }

  // Desktop: Use draggable row
  return <DraggableColumnRow column={column} />;
}
```

### API Route Patterns

[Source: docs/architecture/api-specification.md]

**POST /api/column-config (Add Column):**

```typescript
// src/app/api/column-config/route.ts
import { requireHRAdminAPI } from '@/lib/auth-helpers';
import { addColumnSchema } from '@/lib/validation/column-schema';

export async function POST(request: Request) {
  // Auth check
  const { user } = await requireHRAdminAPI();

  // Validate request
  const body = await request.json();
  const validation = addColumnSchema.safeParse(body);
  if (!validation.success) {
    return NextResponse.json(
      { error: validation.error.flatten() },
      { status: 400 }
    );
  }

  // Get max display_order
  const { data: maxOrder } = await supabase
    .from('column_config')
    .select('display_order')
    .order('display_order', { ascending: false })
    .limit(1)
    .single();

  // Insert new column
  const { data, error } = await supabase
    .from('column_config')
    .insert({
      column_name: validation.data.column_name,
      data_type: validation.data.data_type,
      category: validation.data.category || null,
      is_visible: true,
      is_system: false,
      display_order: (maxOrder?.display_order ?? 0) + 1,
    })
    .select()
    .single();

  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }

  return NextResponse.json({ data }, { status: 201 });
}
```

**PATCH /api/column-config/reorder (Reorder Columns):**

```typescript
// src/app/api/column-config/reorder/route.ts
export async function PATCH(request: Request) {
  const { user } = await requireHRAdminAPI();

  const body = await request.json();
  const { columns } = body; // Array of { id, display_order }

  // Validate columns array
  if (!Array.isArray(columns) || columns.length === 0) {
    return NextResponse.json({ error: 'Invalid columns array' }, { status: 400 });
  }

  // Batch update using transaction
  const updates = columns.map((col) =>
    supabase
      .from('column_config')
      .update({ display_order: col.display_order })
      .eq('id', col.id)
  );

  const results = await Promise.all(updates);

  // Check for errors
  const errors = results.filter((r) => r.error);
  if (errors.length > 0) {
    return NextResponse.json(
      { error: 'Failed to update column order' },
      { status: 500 }
    );
  }

  return NextResponse.json({ success: true }, { status: 200 });
}
```

### Accessibility Requirements

[Source: WCAG 2.1 AA, docs/architecture/accessibility-standards.md]

**Drag-and-Drop Accessibility:**

1. **Keyboard Navigation:**
   - Tab to drag handle
   - Space or Enter to activate drag mode
   - Arrow keys to move item up/down
   - Space or Enter to drop
   - Escape to cancel

2. **Screen Reader Support:**
   - Drag handle has `aria-label="Reorder column"`
   - Live region announces: "Column [name] moved to position [n]"
   - Instructions visible or aria-describedby: "Press Space to start dragging"

3. **Focus Management:**
   - Focus returns to moved item after drop
   - Focus visible (outline) on drag handle
   - Focus trap within modal during drag operation

**Implementation:**

```tsx
<button
  {...listeners}
  {...attributes}
  aria-label={t('dragHandle.aria')}
  aria-describedby="drag-instructions"
  className="focus:outline-blue-500"
>
  ☰
</button>
<span id="drag-instructions" className="sr-only">
  Press Space to start dragging. Use Arrow keys to move. Press Space again to
  drop.
</span>
```

### Testing Standards

[Source: docs/architecture/testing-strategy.md]

**Test Coverage Requirements:**

- Unit tests: 90%+ (validation schemas, utility functions)
- Component tests: 80%+ (modal, drag-and-drop interactions)
- Integration tests: 85%+ (API routes)

**Testing Frameworks:**

- **Vitest** v1.1+ for unit/integration tests
- **React Testing Library** for component tests
- **MSW (Mock Service Worker)** for API mocking

**Critical Test Scenarios:**

1. **Add Column:**
   - Valid column creation succeeds
   - Duplicate column name rejects
   - Non-HR Admin user rejected
   - New column has highest display_order

2. **Reorder Columns:**
   - Drag-and-drop updates display_order correctly
   - Reorder persists after page refresh
   - Employee table columns render in new order
   - Mobile "Move Up/Down" buttons work

3. **Visibility Toggle:**
   - Badge displays correct state (Visible/Hidden)
   - Button text changes (Hide/Show)
   - Hidden column not in employee table
   - Toggling visibility updates badge immediately

4. **Authentication Regression:**
   - All existing auth flows still work
   - No redirect loops introduced
   - Login works on first and subsequent attempts
   - Session persists across page refreshes

### Known Issues and Edge Cases

**Edge Case 1: Concurrent Reordering**
- **Scenario:** Two HR Admins reorder columns simultaneously
- **Solution:** Last write wins (acceptable for column settings)
- **Future:** Add optimistic locking or conflict detection

**Edge Case 2: Deleting Column During Reorder**
- **Scenario:** Column deleted while drag-and-drop in progress
- **Solution:** Refresh column list after any CRUD operation
- **Implementation:** Use React Query or SWR for automatic refetch

**Edge Case 3: Very Long Column Names**
- **Scenario:** Column name exceeds table cell width
- **Solution:** Truncate with ellipsis, show full name on hover
- **CSS:** `overflow: hidden; text-overflow: ellipsis; white-space: nowrap;`

**Edge Case 4: Mobile Drag Conflicts with Scroll**
- **Scenario:** Touch drag interferes with page scroll
- **Solution:** Use "Move Up/Down" buttons on mobile (AC 5)
- **Implementation:** `useMediaQuery('(max-width: 768px)')` to toggle UI

**Edge Case 5: System Columns**
- **Scenario:** User tries to delete system columns (SSN, name, etc.)
- **Solution:** `is_system = true` columns cannot be deleted/hidden
- **UI:** Disable "Hide" button for system columns
- **Note:** This story doesn't implement delete, but future-proofs schema

### Definition of Done

- [ ] All acceptance criteria (1-6) implemented and tested
- [ ] Phase 0 pre-implementation safety checks completed
- [ ] Database migration applied successfully
- [ ] Add Column functionality works (modal, API, persistence)
- [ ] Hide/Show visual feedback displays correctly
- [ ] Drag-and-drop reordering works on desktop
- [ ] Mobile "Move Up/Down" buttons work
- [ ] Employee table respects display_order
- [ ] All translations added (English + Swedish)
- [ ] Unit tests: 90%+ coverage, all passing
- [ ] Integration tests: All API routes tested, all passing
- [ ] Component tests: Modal and drag-and-drop tested
- [ ] Phase 7 authentication regression testing completed (ALL PASS)
- [ ] No auth/routing files modified (verified with git diff)
- [ ] Documentation updated (User Guide, API Docs, Schema Docs)
- [ ] Code review checklist completed
- [ ] Manual end-to-end testing completed successfully
- [ ] No TypeScript errors (`pnpm tsc --noEmit`)
- [ ] No linting errors (`pnpm lint`)
- [ ] Performance: Drag-and-drop smooth (<100ms response)
- [ ] Accessibility: Keyboard navigation works, screen reader compatible
- [ ] Ready for QA review

---

## Testing

### Test File Locations

- `tests/unit/validation/column-schema.test.ts` - Column validation tests
- `tests/integration/api/column-config-add.test.ts` - Add column API tests
- `tests/integration/api/column-config-reorder.test.ts` - Reorder API tests
- `tests/unit/components/add-column-modal.test.tsx` - Modal component tests

### Required Test Coverage

- **Unit Tests:** 90%+ coverage on validation schemas
- **Integration Tests:** All API routes covered (add, reorder, toggle visibility)
- **Component Tests:** Modal interactions, drag-and-drop behavior
- **Regression Tests:** Full authentication flow (see Phase 7)

### Testing Frameworks

- **Vitest** v1.1+ for test runner
- **React Testing Library** for component tests
- **@dnd-kit/core** testing utilities for drag-and-drop
- **MSW** for API mocking

---

## Change Log

| Date       | Version | Description                                    | Author             |
| ---------- | ------- | ---------------------------------------------- | ------------------ |
| 2025-10-31 | 0.1     | Created Story 6.6 from Epic 6                  | Bob (Scrum Master) |
| 2025-11-02 | 0.2     | MAJOR REVISION: Added authentication safeguards after Story 6.6 failure | Sarah (Product Owner) |

---

## Dev Agent Record

### Implementation Summary

*To be completed after implementation*

### File List

*To be completed after implementation*

### Test Results

*To be completed after implementation*

### Debug Log References

*To be completed after implementation*

### Completion Notes

*To be completed after implementation*

---

## QA Results

*To be completed by Quinn (Test Architect) after implementation*

---

**CRITICAL REMINDER:**

Before starting this story, run the full authentication testing checklist. If ANY test fails, STOP and consult PO.

This story should ONLY modify column settings UI and API routes. Zero tolerance for modifications to auth/routing files.

**Success Criteria:**
1. All column management features work perfectly
2. **AND** all authentication flows still work perfectly
3. **AND** no auth/routing files were modified

Failure on #2 or #3 = automatic story failure, regardless of #1 completion.
