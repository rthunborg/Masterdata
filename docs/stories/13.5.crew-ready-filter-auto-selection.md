# Story 13.5: Crew Ready Filter Auto-Selection

## Status

**Pending**

---

## Story

**As a** HR Admin user,  
**I want** the "Crew Ready" filter to automatically select all matching employees when activated, and unselect all when deactivated or when another filter is chosen,  
**so that** I can quickly export all crew-ready employees without manual selection.

---

## Acceptance Criteria

**Given** I am on the dashboard with no filter active  
**When** I activate the "Crew Ready" filter  
**Then** all employees matching the crew ready criteria are automatically selected  
**And** all matching employee rows show the greyish tint  
**And** all matching employee checkboxes are checked  
**And** the employee count reflects the number of selected employees

**Given** I have the "Crew Ready" filter active with employees auto-selected  
**When** I click on any individual employee checkbox to uncheck it  
**Then** that specific employee becomes unselected  
**And** the "Crew Ready" filter remains active  
**And** other auto-selected employees remain selected

**Given** I have the "Crew Ready" filter active with employees auto-selected  
**When** I activate a different filter (e.g., "Show Terminated")  
**Then** all previously selected employees are automatically unselected  
**And** the "Crew Ready" filter is deactivated  
**And** the new filter becomes active  
**And** no employees are selected by default

**Given** I have the "Crew Ready" filter active  
**When** I click the "Crew Ready" filter again to deactivate it  
**Then** all auto-selected employees are automatically unselected  
**And** the filter is deactivated  
**And** the view returns to showing all employees (based on other active filters)

**Given** I activate the "Crew Ready" filter  
**When** the system determines which employees are crew ready  
**Then** crew ready criteria are: all boolean masterdata fields (except `hotel_required`) are `true`, and `loneiva` (salary level) enum field has a value set  
**And** only employees meeting ALL these criteria are selected

**Given** I create a new employee  
**When** the employee record is created  
**Then** the `loneiva` field is initialized as empty/null (no value set)  
**And** the employee does NOT meet crew ready criteria until `loneiva` is set

---

## Tasks / Subtasks

### Phase 1: Implement Crew Ready Auto-Selection Logic (AC: All)

- [ ] **Task 1.1: Create Crew Ready Query Function**
  - [ ] Open `src/lib/services/crewing-validation.ts` or create new utility
  - [ ] Create function: `getCrewReadyEmployeeIds()` that returns array of employee IDs
  - [ ] Use existing `canEditCrewingDone` logic to determine crew ready status
  - [ ] Query all employees and filter by crew ready criteria
  - [ ] Return array of IDs: `string[]`
  - [ ] Test: Function returns correct employee IDs

- [ ] **Task 1.2: Integrate Auto-Selection with Crew Ready Filter**
  - [ ] Open `src/app/dashboard/page.tsx`
  - [ ] Locate crew ready filter handler (or create if doesn't exist)
  - [ ] When crew ready filter activates: Call `getCrewReadyEmployeeIds()` and set selection
  - [ ] Update selection state: `setSelectedEmployeeIds(new Set(crewReadyIds))`
  - [ ] Test: Activating crew ready filter selects matching employees

- [ ] **Task 1.3: Implement Auto-Unselection on Filter Change**
  - [ ] Update filter change handlers to clear selection when switching filters
  - [ ] When crew ready filter deactivates: Clear selection `setSelectedEmployeeIds(new Set())`
  - [ ] When another filter activates: Clear selection
  - [ ] Test: Switching filters clears selection

- [ ] **Task 1.4: Allow Manual Selection Override**
  - [ ] Ensure individual checkbox clicks still work when crew ready filter is active
  - [ ] User can uncheck individual employees even when auto-selected
  - [ ] User can check additional employees if needed
  - [ ] Test: Manual selection works alongside auto-selection

- [ ] **Task 1.5: Verify Loneiva Default Value**
  - [ ] Open `src/components/dashboard/add-employee-modal.tsx` or employee creation logic
  - [ ] Verify `loneiva` field defaults to `null`/empty on employee creation
  - [ ] Test: New employees don't meet crew ready criteria until `loneiva` is set
  - [ ] Update if needed to ensure default is `null`

---

## Dev Notes

### Crew Ready Criteria

Crew ready criteria: All boolean fields (except `hotel_required`) = `true` AND `loneiva` has value.

Boolean fields to check:
- `isps`
- `photo`
- `origo`
- `mail_lon`
- `bankuppgifter`
- `li`
- `passport`
- `kvitto_c17_18`
- `c17`

Exclude `hotel_required` from crew ready criteria.

### Auto-Selection Implementation

```typescript
const handleCrewReadyFilterChange = async (active: boolean) => {
  if (active) {
    // Get crew ready employee IDs
    const crewReadyIds = await getCrewReadyEmployeeIds();
    setSelectedEmployeeIds(new Set(crewReadyIds));
  } else {
    // Clear selection when filter deactivates
    setSelectedEmployeeIds(new Set());
  }
};
```

### Filter Change Handler Pattern

When any filter changes (except crew ready), clear selection:
```typescript
const handleFilterChange = (filterType: string, value: boolean) => {
  // Clear selection when switching to non-crew-ready filter
  if (filterType !== 'crewReady' && value) {
    setSelectedEmployeeIds(new Set());
  }
  // ... rest of filter logic
};
```

### Source Tree References

- Crewing validation: `src/lib/services/crewing-validation.ts`
- Dashboard page: `src/app/dashboard/page.tsx`
- Add employee modal: `src/components/dashboard/add-employee-modal.tsx`
- Selection state: From Story 13.2

---

## Testing Requirements

**CRITICAL:** All tests in the entire project must pass before marking this story as "Ready for Review" or "Done".

**Test Organization:** All new tests must be placed in folders named for the epic and story number:
- Unit tests: `tests/unit/epic-13/story-13.5/`
- Integration tests: `tests/integration/epic-13/story-13.5/`
- E2E tests: `tests/e2e/epic-13/story-13.5/`
- This organization ensures that when multiple developers work on different stories, it's easy to identify which tests belong to which story

### Unit Tests

- [ ] **Test: Crew Ready Employee ID Query**
  - [ ] Create test: `tests/unit/services/crew-ready-auto-selection.test.ts`
  - [ ] Test: `getCrewReadyEmployeeIds()` returns correct IDs
  - [ ] Test: Only employees meeting ALL criteria are included
  - [ ] Test: Employees without `loneiva` are excluded
  - [ ] Test: Employees with any false boolean field are excluded
  - [ ] Test: `hotel_required` is not part of criteria

- [ ] **Test: Auto-Selection Logic**
  - [ ] Test: Activating crew ready filter selects matching employees
  - [ ] Test: Deactivating crew ready filter clears selection
  - [ ] Test: Switching to another filter clears selection
  - [ ] Test: Manual selection override works

### Integration Tests

- [ ] **Test: Crew Ready Auto-Selection Integration**
  - [ ] Create test: `tests/integration/dashboard/crew-ready-auto-selection.test.ts`
  - [ ] Test: Crew ready filter activates and selects employees
  - [ ] Test: Selected employees show greyish tint
  - [ ] Test: Checkboxes are checked for selected employees
  - [ ] Test: Switching filters clears selection

### E2E Tests

- [ ] **Test: Crew Ready Auto-Selection Workflow**
  - [ ] Create test: `tests/e2e/crew-ready-auto-selection.spec.ts`
  - [ ] Test: User activates crew ready filter and employees are auto-selected
  - [ ] Test: User can uncheck individual employees
  - [ ] Test: User switches to another filter and selection clears
  - [ ] Test: User deactivates crew ready filter and selection clears
  - [ ] Test: New employees don't meet crew ready criteria

### Manual Testing Checklist

- [ ] Activating crew ready filter selects matching employees
- [ ] Selected employees show greyish tint and checked checkboxes
- [ ] Individual checkboxes can be unchecked manually
- [ ] Switching to another filter clears selection
- [ ] Deactivating crew ready filter clears selection
- [ ] Crew ready criteria are correct (all booleans + loneiva)
- [ ] New employees don't meet crew ready criteria
- [ ] `hotel_required` is not part of criteria

### Test Execution

Before marking story as complete:
1. Run all unit tests: `pnpm test`
2. Run all integration tests: `pnpm test:integration`
3. Run all E2E tests: `pnpm test:e2e`
4. Verify ALL tests in the project pass (not just tests for this story)
5. Fix any failing tests before proceeding

---

## Prerequisites

- Story 13.2 (Add Employee Row Selection Checkboxes) - Selection functionality must exist
- Story 8.5 (Crewing/Done Field Conditional Logic) - Crew ready logic must exist

