# Story 13.3: Implement Row Click Selection

## Status

**Pending**

---

## Story

**As a** HR Admin user,  
**I want** to click anywhere on an employee row (except buttons or input fields) to select/deselect that employee,  
**so that** I can quickly select employees without needing to target a small checkbox.

---

## Acceptance Criteria

**Given** I am viewing an employee row that is not selected  
**When** I perform a single left click on the row (not on a button, not on an input field, not on a link)  
**Then** the row becomes selected  
**And** the row receives a greyish tint  
**And** the selection checkbox (if visible) becomes checked  
**And** the employee is added to the selected state

**Given** I am viewing an employee row that is already selected  
**When** I perform a single left click on the row (not on a button, not on an input field, not on a link)  
**Then** the row becomes unselected  
**And** the greyish tint is removed  
**And** the selection checkbox (if visible) becomes unchecked  
**And** the employee is removed from the selected state

**Given** I have multiple employees selected  
**When** I click on a different unselected employee row  
**Then** that new row becomes selected  
**And** previously selected rows remain selected  
**And** all selected rows show the greyish tint

**Given** I click on a button within an employee row (e.g., "Edit", "Archive", action menu)  
**When** the button click occurs  
**Then** the row selection does NOT change  
**And** the button's normal action executes (e.g., opens edit modal)

**Given** I click on an input field within an employee row (e.g., inline editable field)  
**When** the input field receives focus  
**Then** the row selection does NOT change  
**And** the input field behaves normally (allows editing)

**Given** I click on a link within an employee row  
**When** the link is clicked  
**Then** the row selection does NOT change  
**And** the link navigation occurs normally

**Given** I am on mobile viewing an employee card  
**When** I tap on the card (not on buttons or input fields)  
**Then** the card becomes selected with greyish tint  
**And** the selection checkbox (if visible) becomes checked

---

## Tasks / Subtasks

### Phase 1: Add Row Click Handler (AC: All)

- [ ] **Task 1.1: Implement Row Click Detection**
  - [ ] Open `src/components/dashboard/employee-table.tsx`
  - [ ] Add `onClick` handler to `TableRow` component
  - [ ] Implement event delegation to detect clicks on row vs. interactive elements
  - [ ] Check `event.target` to determine if click is on button, input, or link
  - [ ] Use pattern: `if (event.target.closest('button, input, a, [role="button"]')) return;`
  - [ ] Only trigger selection if click is NOT on interactive element

- [ ] **Task 1.2: Integrate with Selection State**
  - [ ] Connect row click handler to `toggleEmployeeSelection` function
  - [ ] Ensure row click and checkbox click both update the same selection state
  - [ ] Test: Clicking row selects/deselects employee
  - [ ] Test: Clicking checkbox also works (both methods work)

- [ ] **Task 1.3: Prevent Selection on Interactive Elements**
  - [ ] Identify all interactive elements in row: buttons, inputs, links, dropdowns, action menus
  - [ ] Ensure clicks on these elements don't trigger selection
  - [ ] Test: Clicking "Edit" button doesn't change selection
  - [ ] Test: Clicking inline editable field doesn't change selection
  - [ ] Test: Clicking action menu doesn't change selection

- [ ] **Task 1.4: Implement Mobile Card Click Selection**
  - [ ] Locate mobile employee card component
  - [ ] Add same click handler logic to card component
  - [ ] Ensure touch events work correctly on mobile
  - [ ] Test: Tapping card selects/deselects employee
  - [ ] Test: Tapping buttons/inputs in card doesn't change selection

- [ ] **Task 1.5: Add Visual Feedback**
  - [ ] Ensure greyish tint appears immediately on row click
  - [ ] Ensure checkbox state updates immediately
  - [ ] Test: Visual feedback is immediate and smooth

---

## Dev Notes

### Event Delegation Pattern

Use event delegation to detect clicks on row vs. interactive elements:
```typescript
const handleRowClick = (event: React.MouseEvent<HTMLTableRowElement>, employeeId: string) => {
  // Check if click is on an interactive element
  const target = event.target as HTMLElement;
  if (target.closest('button, input, a, [role="button"], [role="menuitem"]')) {
    return; // Don't change selection
  }
  
  // Toggle selection
  toggleEmployeeSelection(employeeId);
};
```

### Interactive Elements to Exclude

- Buttons: `button`, `[role="button"]`
- Input fields: `input`, `textarea`, `select`
- Links: `a`
- Dropdowns: `[role="menu"]`, `[role="menuitem"]`
- Action menus: Any element with click handlers that perform actions

### Source Tree References

- Employee table: `src/components/dashboard/employee-table.tsx`
- Selection state: From Story 13.2

---

## Testing Requirements

**CRITICAL:** All tests in the entire project must pass before marking this story as "Ready for Review" or "Done".

**Test Organization:** All new tests must be placed in folders named for the epic and story number:
- Unit tests: `tests/unit/epic-13/story-13.3/`
- Integration tests: `tests/integration/epic-13/story-13.3/`
- E2E tests: `tests/e2e/epic-13/story-13.3/`
- This organization ensures that when multiple developers work on different stories, it's easy to identify which tests belong to which story

### Unit Tests

- [ ] **Test: Row Click Handler**
  - [ ] Create test: `tests/unit/components/employee-row-click-selection.test.tsx`
  - [ ] Test: Clicking row selects employee
  - [ ] Test: Clicking selected row deselects employee
  - [ ] Test: Clicking button in row doesn't change selection
  - [ ] Test: Clicking input field in row doesn't change selection
  - [ ] Test: Clicking link in row doesn't change selection

- [ ] **Test: Event Delegation Logic**
  - [ ] Test: `closest()` correctly identifies interactive elements
  - [ ] Test: Row click is prevented when clicking interactive elements
  - [ ] Test: Row click works when clicking non-interactive areas

### Integration Tests

- [ ] **Test: Row Click Integration**
  - [ ] Create test: `tests/integration/components/employee-row-click-integration.test.tsx`
  - [ ] Test: Row click updates selection state
  - [ ] Test: Row click updates checkbox state
  - [ ] Test: Row click updates visual tint
  - [ ] Test: Interactive elements still work correctly

### E2E Tests

- [ ] **Test: Row Click Selection Workflow**
  - [ ] Create test: `tests/e2e/employee-row-click-selection.spec.ts`
  - [ ] Test: User can click row to select employee
  - [ ] Test: User can click selected row to deselect
  - [ ] Test: Clicking "Edit" button doesn't change selection
  - [ ] Test: Clicking inline field doesn't change selection
  - [ ] Test: Multiple rows can be selected via clicks
  - [ ] Test: Row click works on mobile cards

### Manual Testing Checklist

- [ ] Clicking row selects employee and shows greyish tint
- [ ] Clicking selected row deselects employee
- [ ] Clicking "Edit" button doesn't change selection
- [ ] Clicking inline editable field doesn't change selection
- [ ] Clicking action menu doesn't change selection
- [ ] Multiple rows can be selected via clicks
- [ ] Row click works on mobile cards
- [ ] Checkbox and row click both work (either method selects)

### Test Execution

Before marking story as complete:
1. Run all unit tests: `pnpm test`
2. Run all integration tests: `pnpm test:integration`
3. Run all E2E tests: `pnpm test:e2e`
4. Verify ALL tests in the project pass (not just tests for this story)
5. Fix any failing tests before proceeding

---

## Prerequisites

- Story 13.2 (Add Employee Row Selection Checkboxes) - Selection state management must exist

