# Story 22.15 next production gates — 2026-09-23

> **Read-only refresh — 2026-09-24.** The same seven strict production catalog groups fail, the 48 filters remain orphaned, and history is still absent. Realtime, Data API and broad database ingress remain open. A second source-bound probe shows the cron/pg_net-named workers preloaded and targeting this database but without either extension or its usual job/request table. The isolation-setting plan must require a fresh pre-write worker check and stop on any newly present job/request substrate. The exact target/TLS-bound evidence and receipt identities are [recorded here](production-readonly-cutover-refresh-2026-09-24.md). Production remains paused/no-go; no hosted change occurred.

> **Owner update — 2026-09-24.** The owner verified the staging application, waived calendar maintenance-window coordination while production is unused, named Rasmus rollback owner, and reported local backups with a verified restore. The backup contents and location are not needed for this preparation. Confirm backup freshness and record the controlled cutover start before any hosted change. The first step below incorporates this completed input. [Current owner input](production-owner-cutover-input-2026-09-24.md).

> **Later owner confirmation and network preflight — 2026-09-24.** The owner attests that the restore-tested backup is newer than the last production write and separately approves proceeding with temporary isolation work. Two independent IPv4 egress lookups on the operator computer agreed; no IPv6 egress path was available. The IPv4 candidate is stored in encrypted local inputs, outside this repository. The isolation gate now supports a strictly proved IPv4-operator/IPv6-deny-all profile as well as its prior dual-stack profile. Neither profile passes without independent non-operator IPv4 and IPv6 denial probes. This is preparation, not proof that production is isolated; no hosted setting or database row changed.

The reviewed PR #115 was merged into staging `3fb3d72a3a26672d91be1a98a51e964b804fdd02`; its merge tree equals the reviewed head `a4f663dd04b43694deeebffcdeaea5d7aba16ec4`. That post-merge staging read-only proof was 68/68 migration history with no pending versions, strict catalog 16/16, the known scoped advisor profile, and unchanged repayment, permission and audit baselines. PR #116 subsequently merged documentation only into staging `3d537699b884429e530848e7fda044a088ad257d`. Main remains `822350986f4c023948a7bbf490ddffc371185c4a`.

Production remains **paused and no-go**. The September 23 findings were refreshed on September 24: migration history is absent, 48/48 saved filters reference deleted Auth users, seven strict catalog groups fail, 55 proposed history repairs remain unproved and unsigned, and 13 immutable forward executes remain proposed. Realtime remains enabled, the Data API exposes public and GraphQL public schemas, broad IPv4/IPv6 ingress remains, and existing database sessions are not drained. Database isolation is not proved. The September 24 read-only Vercel receipt matched the recorded static production pause target, showed disabled automatic domain assignment, and counted zero active cron definitions. No hosted write, setting change, deployment or reopening occurred in this preparation.

The next proposed database window is a sequence of distinct gates, not one unchecked push:

1. Record the owner's completed staging application verification, waiver of calendar maintenance-window coordination, named rollback owner, and attestation that the restore-tested local database/schema backup is newer than the last production write. Reconfirm the attestation immediately before the first hosted isolation change and record the controlled cutover start. If freshness changes or becomes uncertain, stop until a newly captured backup has been restore-verified under a separately approved method. Any backup workflow dispatch needs its own authorization.
2. Capture the current private settings, operator egress addresses, publication membership, Realtime client reports, direct database writer/session inventory, and a representative non-operator Realtime probe. Bind every read to the production target and approved TLS/tooling. Keep identifiers, address ranges, credentials, client topics and row data in private storage only.
3. The owner separately approved proceeding with temporary isolation. Before changing any setting, bind the exact private operator egress and rollback state, recheck the worker/job/queue substrate, and make the controls and independent probes executable. Retain the static production pause; block non-operator application ingress and keep application jobs/workers stopped; disable Realtime service and Data API; restrict direct PostgreSQL and pooler ingress to the approved operator IPv4 `/32` and deny IPv6 entirely when the operator has no IPv6 route; drain or terminate existing non-operator sessions under the approved window. Prove non-operator IPv4/IPv6 denial, old Realtime disconnect, fresh reconnect rejection, zero connected clients and operator database access before any cleanup, history repair or migration apply. A partial or uncertain isolation result is no-go.
4. After isolation proof, seek separate approval for the reviewed single-transaction deletion of exactly the 48 orphan saved filters, followed by independent post-commit preservation proof. The draft SQL SHA-256 is `39d3505a06402761b58480bbf608f9f52771d33dc28d9d09babf6e5cfa6941a2`; it is preparation, not a hosted execution command.
5. Prove each of the 55 migration-defined material effects with target-bound read-only evidence and obtain the complete technical-owner signed ledger. Only then seek separate authorization for the exact 55-version history repair and immediately reconcile history. The static 55-row source inventory is not material-effect proof and provides no repair allowlist.
6. Reconfirm the exact 13-version forward order with a reviewed include-all dry run and execution admission. Standing authorization covers reviewed forward migration applies only after those prerequisites. The current generic wrapper deliberately rejects non-dry-run production apply, so no production apply is executable from this candidate. Keep Realtime and Data API disabled through final strict catalog, advisor, aggregate, direct-role and preservation proofs.
7. Restoration of hosted controls, staging-to-main merge, any production-target deployment, and reopening each require their own explicit decision. Retain the static page/API/mutation pause and job shutdown throughout the database rollout.

The staging application signoff, rollback owner and calendar-window disposition are recorded in [current owner input](production-owner-cutover-input-2026-09-24.md). The owner later confirmed backup freshness and separately approved temporary isolation work. Execution still requires the exact private setting/egress/rollback pre-state and live denial probes; this document does not classify isolation as passed. No approval is inferred for a new backup or workflow dispatch, cleanup, history repair, main merge, deployment or reopening.
