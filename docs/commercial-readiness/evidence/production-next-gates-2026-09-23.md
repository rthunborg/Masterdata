# Story 22.15 next production gates — 2026-09-23

The reviewed PR #115 is merged into staging `3fb3d72a3a26672d91be1a98a51e964b804fdd02`. Its merge tree equals the reviewed head `a4f663dd04b43694deeebffcdeaea5d7aba16ec4`. The new post-merge staging read-only proof is 68/68 migration history with no pending versions, strict catalog 16/16, the known scoped advisor profile, and unchanged repayment, permission and audit baselines. Main remains `822350986f4c023948a7bbf490ddffc371185c4a`.

Production remains **paused and no-go**. The latest read-only production observations on 2026-09-23 found an absent migration-history table, 48/48 saved filters referencing deleted Auth users, seven strict catalog failures, 55 unproved and unsigned proposed history repairs, and 13 proposed immutable forward executes. Realtime was enabled, Data API exposed public and GraphQL public schemas, broad IPv4/IPv6 ingress remained, and existing database sessions/workers were not drained. Database isolation was not proved. The same-day read-only Vercel receipt matched the recorded static production pause target, showed disabled automatic production-domain assignment, and counted zero active cron definitions. The Vercel connector's direct project read returned `INVALID_ARGUMENT` later; that failed connector call is not a fresh pause proof. No hosted write, setting change, deployment or reopening occurred in the PR #115 or post-merge proof work.

The next proposed database window is a sequence of distinct gates, not one unchecked push:

1. Obtain owner staging application verification for this exact merged candidate and an owner-approved maintenance window with a named rollback owner. Obtain and record separate owner authorization for the exact production-day backup method and any workflow dispatch before taking it, then take the fresh production-day backup and complete the runbook-prescribed restore verification in the approved window immediately before the first hosted isolation change; earlier backups remain useful recovery material but do not satisfy this timed gate.
2. Capture the current private settings, operator egress addresses, publication membership, Realtime client reports, direct database writer/session inventory, and a representative non-operator Realtime probe. Bind every read to the production target and approved TLS/tooling. Keep identifiers, address ranges, credentials, client topics and row data in private storage only.
3. Seek separate owner authorization for the precise temporary isolation change set and rollback: retain the static production pause; block non-operator application ingress and keep application jobs/workers stopped; disable Realtime service and Data API; restrict direct PostgreSQL and pooler ingress to the approved operator egress; drain or terminate existing non-operator sessions under the approved window. Prove non-operator denial, old Realtime disconnect, fresh reconnect rejection, zero connected clients and operator database access before any cleanup, history repair or migration apply. A partial or uncertain isolation result is no-go.
4. After isolation proof, seek separate approval for the reviewed single-transaction deletion of exactly the 48 orphan saved filters, followed by independent post-commit preservation proof. The draft SQL SHA-256 is `39d3505a06402761b58480bbf608f9f52771d33dc28d9d09babf6e5cfa6941a2`; it is preparation, not a hosted execution command.
5. Prove each of the 55 migration-defined material effects with target-bound read-only evidence and obtain the complete technical-owner signed ledger. Only then seek separate authorization for the exact 55-version history repair and immediately reconcile history. The static 55-row source inventory is not material-effect proof and provides no repair allowlist.
6. Reconfirm the exact 13-version forward order with a reviewed include-all dry run and execution admission. Standing authorization covers reviewed forward migration applies only after those prerequisites. The current generic wrapper deliberately rejects non-dry-run production apply, so no production apply is executable from this candidate. Keep Realtime and Data API disabled through final strict catalog, advisor, aggregate, direct-role and preservation proofs.
7. Restoration of hosted controls, staging-to-main merge, any production-target deployment, and reopening each require their own explicit decision. Retain the static page/API/mutation pause and job shutdown throughout the database rollout.

The immediate owner input needed is staging application signoff and a proposed maintenance window. The temporary isolation action will be submitted for a separate approval only after a fresh private pre-state and operator-egress/rollback record make the exact settings change reviewable. No current approval is being inferred for production backup, restore verification or workflow dispatch, cleanup, history repair, settings, main merge, deployment or reopening.
