# Production read-only cutover refresh — 2026-09-24

The source-bound support collector was rebound to staging `3d537699b884429e530848e7fda044a088ad257d`; main remained `822350986f4c023948a7bbf490ddffc371185c4a`. Its source and dependency pins passed before connection, and the 13 production-profile diagnostic tests passed. The private Windows PowerShell 5.1 launcher verified the encrypted production inputs, reviewed Supabase CLI 2.115.0, reviewed psql 17.11, SSL root, CLI link, connection mode and three-way target binding. SQL collectors used bounded explicit read-only transactions and redacted their output. The seven local support receipts and their SHA-256 hashes are held in `C:\DEV\hr-masterdata-cutover-readonly-20260924-v4`; raw definitions, private target values, credentials, addresses and row data are not in this repository.

At `2026-09-24T08:35:40.42Z`, the production profile collection succeeded with no hosted write. Migration history remains physically absent. Saved filters are 48 total and 48 orphaned, with zero empty or overlength names. Audit remains 1,025 rows, and the permission baseline remains 61 rows. The complete approved aggregate profile matched the September 23 redacted capture. This is an observed-state result, not proof of each migration's material effect or a repair authorization. The profile receipt SHA-256 is `387423ff395ac4437c14d2428f52d39ef9271576260a815f5485036869a3cab7`.

The repository's unmodified `production_pre_apply` catalog phase was run only through `verify-production-baseline-catalog.mjs`. It collected all 16 checks and still fails exactly seven: `dietary_columns_and_permissions`, `represented_column_contracts`, `represented_function_contracts`, `represented_policy_contracts`, `represented_trigger_contracts`, `user_filters_objects`, and `user_filters_trigger_function_contract`. It is **9/16, not a passed gate**. Receipt SHA-256: `97ac0fc592ceb3967f44d4fdf048e535f544ba626ea69160c32e11d8a9e67cc9`.

Read-only isolation inventories collected at 08:37–08:39 UTC also remain no-go. There are zero deployed Edge Functions, zero enabled known Auth hooks and zero unknown Auth hooks, but Realtime is not suspended and the Data API exposes both `public` and `graphql_public`. The database network allowlist is applied but its one IPv4 and one IPv6 entries each permit all addresses. Five client backend sessions were observed. The expanded writer inventory classifies their roles, with zero enabled subscriptions and zero replication slots; it still reports two nonclient backend types and one login role outside its base list. A separate bounded candidate classifier finds that the remaining login role has the exact managed CLI-login membership profile. It observes one `pg_cron launcher` and one semver-named `pg_net` worker, but finds **neither extension installed in the connected database**. Its zero `unclassifiedBackendCount` is only a name-pattern result: under the documented extension-qualified role/backend contract, both workers remain unresolved as potential internal write or outbound paths. The point-in-time zero subscriptions/slots cannot establish future quiescence. These observations do **not** prove traffic isolation or authorize session termination. Reverify the seven already-disabled Auth hook controls; separately change and prove Realtime, Data API, network ingress, existing sessions and direct write paths during the cutover. Do not infer that disabling HTTP and direct database ingress stops the unresolved background workers.

The redacted isolation, platform, writer, extended-classifier and management receipts have SHA-256 values `02dc86a01ca2d7a5a2a24f681207999b3e3e0bc8edafca6a1a9dccf9d4ae5528`, `b056331e0c9488141fcf3ab7fa9fb34498482c193ab0d3ce771cd053261435ec`, `071e3dc6950364a403b95f5f6576b8355b2b3e14514e3a3c5d1383ed062f7e61`, `1d2fdd9fe265c127b73409995b270bdbc046c759948836e293b5726b1900a6bb`, and `01eaf567d0fbf1d7ec38cbfa2d31fae4bc5a31e22e6a11bfafc4920e82ab70c6`, respectively. These are evidence identities, not allowlists or proof of safe execution.

At `2026-09-24T08:53:54.294Z`, a second source-pinned read-only probe narrowed the worker scope. Both libraries are preloaded; both configured worker database targets match the connected production database; one launcher and one network worker appear there, with zero matching workers attached to another database. Neither extension, the `cron.job` table, nor the `net.http_request_queue` table exists there. `cron.launch_active_jobs` remains **on**; the absence of a job table, not that setting, is the current reason no scheduled job is represented in the connected database. The 68 immutable migration files contain no pg_cron/pg_net or queue/job-table reference; their only extension creations are `uuid-ossp` and `pgcrypto`. The private worker-scope receipt SHA-256 is `2b5616dcb0ba0bfe6998b43883577390e05e4239029da956b6947d6fff3a84c9`. The local parser passed 2/2 negative/positive tests, all 37 support-file pins passed, source integrity passed, and the SQL passed the reviewed read-only transaction guard. This narrows the two workers to **preloaded but without their usual job/request tables in the observed database**. It does not prove an immutable service shutdown or identify the exact installed worker binary behavior; repeat the catalog/configuration proof before the first hosted mutation and after any schema or setting change. If an extension or queue/job table appears, stop and review the writer path and isolation change set before proceeding. [pg_net's current worker source](https://github.com/supabase/pg_net/blob/master/src/worker.c) skips request processing when its extension tables are absent; [pg_cron's current source](https://github.com/citusdata/pg_cron/blob/main/src/pg_cron.c) launches tasks from its job metadata. Those upstream sources inform this inference but are not a hash-bound identification of the installed binaries.

The current production pause was independently inspected through read-only Vercel CLI on the same date: the production target matched the private pause deployment record, automatic domain assignment was disabled, and active cron definitions were empty. See [owner input and pause result](production-owner-cutover-input-2026-09-24.md). The worker result above is a bounded pre-state classification, not permission to alter managed extensions or workers. A proposed isolation change set must require fresh worker catalog/configuration proof and an explicit stop/deny action if the usual job or request substrate becomes present. No production data, migration history, setting, deployment or pause state was changed. All 55 repair rows remain `UNPROVED` and unsigned; 13 forward migrations remain proposed. Story 22.15 remains in-progress, Epic 23 on hold, and production paused/no-go.
