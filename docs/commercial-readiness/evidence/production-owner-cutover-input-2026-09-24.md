# Production cutover owner input — 2026-09-24

The owner has tested the staging/preview application and reports that it works as intended. The merge of PR #116 into staging `3d537699b884429e530848e7fda044a088ad257d` changed documentation only after the tested application candidate. Production is not in use, so the owner does not require a calendar maintenance window or timezone coordination. Rasmus is the rollback owner.

The owner reports that production database and schema backups are stored locally and that a restore was verified. The backup location and contents are intentionally not recorded here. This is an owner attestation; no new machine-verified backup or restore receipt was obtained in this preparation. The runbook's fresh-backup gate remains mandatory: before the first hosted change, confirm that the backup was captured after the last production write and that the restore verification used that same backup, then record the controlled cutover start. If either fact cannot be confirmed, stop and obtain a newly captured, restore-verified backup under a separately approved method. A backup workflow dispatch would be a distinct action requiring its own authorization.

The owner accepts losing the 48 orphan saved filters if the reviewed one-transaction cleanup remains necessary. This disposition does not authorize running that cleanup. Its exact target-bound preconditions, full technical isolation, separately approved hosted deletion and independent post-commit proof remain required. No other production data is approved for deletion, reset or purge.

The remaining short path is one reviewed package for the source-specific 55-row material-effect proof, exact cleanup, isolation and protected 13-file apply admission. A matching migration-history label alone is not evidence that a historical data transformation ran. The 55 rows remain `UNPROVED` and unsigned until a fresh read-only effect matrix supports each row and the technical owner signs the batch. The seven strict production catalog failures remain real; final `post_apply` must pass all 16 groups.

Production stays paused and no-go. The separately authorized gates are isolation/settings and session drain, one cleanup transaction, the exact 55-version history repair, restoration of hosted controls, main merge, production deployment and reopening. The owner's standing authorization covers reviewed forward migration applies after their technical prerequisites. Story 22.15 remains in-progress and Epic 23 on hold.

A fresh read-only Vercel CLI inspection at `2026-09-24T08:24:07.335Z` matched the privately recorded production pause deployment and project, confirmed automatic custom-domain assignment disabled, and found zero active cron definitions. The redacted receipt is held in the local support directory as `vercel-cli-readonly-20260924.json` (CLI 59.11.7; reviewed CLI file SHA-256 `2dd6e7c273a24bf4317af867d9b7bacb4db35487b42ea77912e2e7c33fa0c152`). No deployment or Vercel setting changed. Reverify the pause immediately before any main merge or production-targeted deployment.
