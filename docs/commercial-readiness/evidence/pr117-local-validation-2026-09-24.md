# PR #117 local validation — 2026-09-24

The tested implementation is `d99b3b7a9f44682896c359058f7569cc66d86dcc`, based on staging `3d537699b884429e530848e7fda044a088ad257d`. The guarded disposable Supabase fixture was restarted through the resource guard and independently passed all 16 strict `post_apply` catalog checks before the suite ran. A fresh ignored `.env.test` in the isolated checkout selected that verified synthetic fixture; it contains no production input. The fixture and test configuration remain outside the PR.

The first exact `npx vitest run` attempt, before selecting the guarded fixture, exited 1 after 265.36 seconds: 3,997 passed, 47 skipped, two failed. Its existing configured local database failed the strict catalog groups `dietary_columns_and_permissions` and `represented_policy_contracts`; that was a local fixture mismatch, not a passing gate. The guarded-fixture rerun of the same command exited **0** after **172.56 seconds**: **3,999 passed, 47 skipped, zero failed** across 354 test files (349 passed, five skipped). The private full log SHA-256 is `6ff50e4ca991a61e33d66c8923c1029d5a451cbf18258ebadff52ecd9a63a2e8`. Of the 47 skips, 42 are four separately admitted Story 22.15 fixture/CLI suites (8 post-apply, six cleanup-prerequisite, eight CLI-matrix, 20 saved-filter reconciliation); five require the HR-admin export application base URL. They are not counted as passing evidence.

The exact full `npx playwright test` also ran inside the resource guard against the same synthetic fixture. It exited **0** after **1,070.434 seconds**: **163 passed, 47 skipped, zero failures and zero test errors**. The current JUnit report contains 210 cases, and its 47 skip identities match the prior reviewed report exactly; no skipped case was reclassified as a pass. The private full log SHA-256 is `eb5f2a026333c0fb0313cb2b026e252624500b1aab620d4936f230337e8023f0`, independently matching the runner receipt.

TypeScript `npx tsc --noEmit` exited 0. Full `npx eslint .` exited 0 with zero errors and 296 existing warnings. These checks ran on the same implementation before this result-only documentation update. The ignored synthetic `.env.test` was removed after its generated hash matched. Guard Stop and CloseActor requests succeeded; the final List reported both the Playwright process tree and disposable Compose fixture stopped. No hosted database write, migration repair, cleanup, Vercel setting change, deployment, main merge or production reopening occurred. Production remains paused/no-go; Story 22.15 remains in-progress and Epic 23 on hold.
