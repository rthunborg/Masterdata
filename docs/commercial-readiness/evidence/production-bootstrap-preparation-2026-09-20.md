# Production bootstrap preparation — 2026-09-20

## Current boundary

This combines local preparatory evidence with the authorized production read-only permission collection recorded in the [redacted machine receipt](production-bootstrap-preparation-2026-09-20.json). Production remains paused and no-go; Story 22.15 remains in-progress and Epic 23 remains on hold. No production database write, history repair, cleanup, setting change, deployment, main merge, or reopening occurred. The production non-dry-run CLI block is unchanged.

The component under preparation can materialize and verify an offline 13-file forward subset from an immutable reviewed source, but it cannot bootstrap a target. Its synthetic rehearsal and point-in-time private-workspace smoke do not complete admission. Cleanup and isolation collectors are not implemented, and the complete 55-row repair ledger remains **UNPROVED** and unsigned.

## Local evidence retained

- Schema reconstruction attempts 1–4 are retained failed observations. Attempt 5 matched all 14 captured metadata groups. That proves only the bounded schema capture and local reconstruction, not a complete Supabase platform or production admission.
- The declared synthetic post-cleanup derivative applied the ordered 13-file subset in 793 ms, produced exactly 13 history rows, preserved both collected repayment maps and all synthetic preservation predicates, and passed the unchanged strict catalog wrapper 16/16. The observed-orphan negative stopped in 723 ms at the immutable ninth migration after exactly the preceding eight history rows; all 48 synthetic orphan filters remained. It is not authority to clean production data.
- Pinned-CLI transaction/history observations remain limited to their documented synthetic cases. A failed apply still requires fresh physical-state and history inspection; no retry or history repair follows automatically.
- The redacted production permission-profile collection exited 0 in 6,787 ms. It confirmed the previous 61-row baseline is unchanged and recorded the two repayment-map SHA-256 values with four and five represented roles respectively. The receipt retains only fixed-profile booleans, counts, and hashes; it is not a complete admission decision.
- The original `411ab7e5d523a27b9a2829401b6872e6cc3821b1`, `7473bb2e829966a71e52bcbeff6f24e5dc094c9f`, and `3c023edff981e725013505e3b5df13fbe0ad8025` private-workspace results are dated evidence only. The 3c full-Vitest failure and the interrupted 3c Playwright run have no completion result for the current code. They do not weaken strict ACL checks or constitute an ACL waiver.
- Current candidate `00dfc4d086f5d151ef25e0f1b3c2ad9bee0efd8c` retires private preparation on every platform before options access, process/import launch, or approval bypass. The coordinator and PowerShell helper are removed; six unconditional denial/getter-boundary regressions replace the retired functional tests, and admission rejects every private-preparation receipt regardless of flags. Focused coverage passed 27/27, zero skips/failures, in 1.11 seconds. Full Vitest passed 3,562 tests across 326 files, zero skips/failures, exit 0, 109.85 seconds reported (118,674 ms wrapper); TypeScript exited 0, ESLint exited 0 with 296 warnings, and a fresh clean catalog passed 16/16. The private feature is unavailable, not a skip and not an unverified feature: any future private capability requires a separately reviewed protected installation/origin, trusted-owner ACL, read-sharing executable locks, and unforgeable bootstrap origin. Its named staging-preview build also passed (exit 0, 17,414 ms); the earlier checkout preflight failure is retained in the machine receipt. Exact Playwright passed 163 tests with 47 classified skips and zero failures/errors; the staging-preview build passed. Final documentation-head CI and exact-head review remain pending at this snapshot.
- A local synthetic design experiment showed that a read-sharing handle can remain open while a child launch succeeds and write/rename attempts are rejected. It establishes only the feasibility of that locking primitive. It does not prove an owner chain, directory-replacement resistance, bootstrap origin, process-launch compatibility in the intended implementation, or any private or hosted operation.

## Remaining gates

A fresh all-68 migration-plus-seed synthetic clean chain passed on the earlier rehearsal candidate. Current candidate `00dfc4d086f5d151ef25e0f1b3c2ad9bee0efd8c` preserves the exact 68 migration SQL files and seed identity. The clean-chain proof used a separate local application fixture and a Node SQL loader, not the pinned CLI, so it does not replace the CLI rehearsal or a hosted proof.

The next reviewed component must complete private working-copy and bootstrap admission controls, production-profile fixture coverage, exact dry-run and failure classification, and the complete local migration proof. The actual prior private parent failed the strict root-ACL check despite passing the redacted ancestor check; it was left unchanged. The later candidate also retained one failed-closed existing-root classification before a fresh restricted-root smoke passed; neither result weakens ACL policy. Before any target materialization, the private workspace must additionally prove a trusted owner chain, with only narrowly accepted operating-system owners, and a negative untrusted-parent-owner regression. Exact-head review, CI/Vercel checks, and fresh hosted proof remain required for its implementation commit.

The retained `7473bb2` production-dependency audit exited 1 with zero critical/high and one moderate finding, the existing accepted UUID advisory through 2026-09-30. Its all-dependency audit also exited 1 with one critical, 16 high, and 10 moderate findings; those are the already registered development-follow-up advisories. Neither audit is a clean result or a waived release gate. Package and lockfile identities are unchanged at `00dfc4d`.

The 48-filter cleanup, traffic isolation/settings, any history repair, main merge, deployment, and reopening each retain separate owner authorization. The connector project inspection returned `INVALID_ARGUMENT`, but a subsequent authorized read-only Vercel CLI fallback proved that the project and production target match the private pause record, automatic custom-domain assignment is disabled, and active cron definitions are present with count zero. This is a current pause/settings proof; it does not authorize a deployment, settings change, or reopening.

All remaining guard-owned resource Stop requests were accepted; CloseActor and the subsequent List succeeded. List reported owned resources as stopped or stop_requested. These acknowledgments do not claim synchronous shutdown. Saved state is retained, and user-owned resources were not adopted or stopped.

Historical documentation regression at the earlier documentation snapshot passed 29/29 with zero skips/failures, exit 0, 0.939 seconds. All three YAML status files parsed, the frozen specification matched the staging base, and evidence-boundary assertions and diff checks passed. The commit after the tested implementation changes documentation/evidence/status only; its remote checks and final exact-head review remain pending.

Exact full browser verification on `00dfc4d`: **163 passed, 47 skipped, zero failures/errors**, exit 0. Report duration 1007.659636 seconds; wrapper duration 1017141 ms. Every skipped file/title matches the retained inventory: nine notification/cron authorization cases and 38 superseded flows or fixture debt. Skips are not passes. XML SHA-256: `e03f5c91a0ad9dd8e87a9ccf15d72476e80fa2031d434fad87190b82a7d11121`.

The renewed documentation regression passed 29/29 with zero skips/failures, exit 0, 1.01 seconds. YAML/status, frozen-region, protected-file identity, redaction and diff checks passed. Final documentation-head remote checks and review remain pending at this pre-review snapshot.
