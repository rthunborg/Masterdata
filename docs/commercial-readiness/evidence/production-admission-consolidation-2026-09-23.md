# Production admission consolidation — 2026-09-23

This is a preparation record for Story 22.15 on staging source `c31227ad68d91c0a471b611811bc618cbf3535a2`. It does not authorize or record a production mutation. Production remains paused and no-go; Story 22.15 remains in-progress and Epic 23 on hold.

## Fresh read-only classification

Reviewed target, TLS, tool and source checks preceded the hosted reads. The production migration-history table is absent. The immutable baseline therefore has **55 proposed history repairs followed by 13 forward executes**, beginning with `20260314000001` and `20260314000002`. The former staffing function is observed as `SECURITY INVOKER`, so the historical transition to `SECURITY DEFINER` still needs exact pre-execute proof. The production catalog gate reports **9/16 passed and seven failed groups**: `dietary_columns_and_permissions`, `represented_column_contracts`, `represented_function_contracts`, `represented_policy_contracts`, `represented_trigger_contracts`, `user_filters_objects`, and `user_filters_trigger_function_contract`. None is waived or reclassified as a strict pass.

The saved-filter inventory contains 48/48 orphaned records and zero invalid names. The observed audit aggregates remain 1,025 entries with 202 legacy-mapped actors and zero unmapped legacy actors; this is a classification, not an assertion of audit data loss. The scoped repayment and 61-row permission baselines were unchanged. Fourteen schema-only groups matched the encrypted earlier snapshot. These are aggregate and hash observations; no row values or private target identifiers are included here.

The read-only pause inspection found the recorded production pause deployment, disabled automatic production-domain assignment and no active cron definitions. The separate Supabase inspection found no deployed Edge Functions and all seven known Auth hooks disabled. Realtime was not suspended, the Data API still exposed `public` and `graphql_public`, and network restrictions admitted broad IPv4/IPv6 traffic. Existing direct database sessions and worker backends remained. A local PostgreSQL rehearsal confirmed that revoking future `CONNECT` does not terminate existing sessions or prevent an already admitted in-flight write. **Five-plane isolation and session/write drain are not proved.** No hosted setting was changed.

## Preparation in this candidate

The code adds bounded read-only observed-profile and writer collectors, exact profile/failure classification, a pure five-plane isolation decision, a 55-row source-bound proof-batch assessor, and a repair-aware 55-to-68 outcome assessor. The strict catalog verifier remains strict; its new observation export preserves the exact seven failure names. A single-transaction, exact-48 saved-filter cleanup draft locks and rechecks counts and hashes, deletes only the identified orphan set, and requires an independent post-commit read. Its locally verified SQL SHA-256 is `39d3505a06402761b58480bbf608f9f52771d33dc28d9d09babf6e5cfa6941a2`. It is a draft SQL artifact, not an executable hosted cleanup command. A post-commit verification failure is an uncertain state requiring diagnosis, not an invitation to rerun the delete.

The 55-row batch assessor can organize submitted material-effect records for human review, but caller-provided facts and digests are not independent proof or signatures. Every existing ledger row remains `UNPROVED` and unsigned. The pure isolation decision is likewise not a live admission token. The generic Supabase wrapper continues to refuse non-dry-run production apply. No production write runner, settings changer, main merge, deployment or reopening path is enabled by this candidate.

The runbook now orders fresh backup attestation and target proof, complete isolation and drain, separately approved 48-filter cleanup and post-proof, separately approved 55-version history repair and immediate verification, then the 13 forward migrations and strict 16/16 final proof. Before history repair, the exact observed seven-failure profile is a prerequisite to classify the existing state; it is not labeled a strict 16/16 pass. Historical migration SQL and the frozen specification remain untouched.

## Local verification

The guarded local PostgreSQL rehearsal applied the clean 68-migration chain plus seed and passed strict post-apply catalog 16/16. A separate first-58-version fixture supported 28 reconciliation tests, 11 inactive-authorization/catalog tests and six cleanup-prerequisite tests. The synthetic cleanup rehearsal passed the positive 48-orphan case, independent zero-postcheck and a drift refusal that preserved its row. The guarded local app stack exposed Auth and PostgREST at loopback only; eight focused integration files passed 35 tests, with five HR-export tests skipped because no Next origin was running in that focused pass.

The final guarded `npx vitest run` passed **4,027 tests, 13 skipped, zero failed** across 352 passing and two skipped files, exit 0, in 262.58 seconds. Eight skipped CLI-matrix integration cases require their separate protected local admission; five HR-export cases require a separately running Next origin. Those 13 are not passes. The exact guarded `npx playwright test` passed **163, with 47 individually classified skips, zero failures and zero errors**, exit 0, in 1,381.401 seconds. The first browser attempt had stopped in global setup because `127.0.0.1` was rejected as a Next development origin; the passing rerun used `localhost` on the same guard-owned fixture. Playwright test/configuration bytes were unchanged.

Focused migration/catalog checks passed 49/49, and the three pause/production-guard files passed 45/45. TypeScript exited 0. ESLint exited 0 with zero errors and 296 warnings. The staging-preview build exited 0 in 7.73 seconds using only the verified synthetic loopback Supabase fixture; its earlier missing-environment attempt exited 1 and is not counted. The production dependency audit reports zero high/critical findings and one previously accepted `uuid` moderate finding (audit exit 1). An earlier full Vitest attempt skipped 34 database cases because fixture URLs were absent; a subsequent run failed on an incorrect 68-version template and a UI test that passed on focused rerun. The next attempt timed out one deterministic package-materialization case under load; both materialization cases now have 180-second limits, and the final full run above passed. These failed attempts remain historical, not passing evidence.

All local fixtures are synthetic and guarded. They do not prove hosted cleanup or migration effects. The exact locally tested implementation commit is `3afb59cab6b0cdd1b3ca2c2a36b0b9336c149a77`; the subsequent evidence-identity amendment changes documentation only. The PR's final head, review and CI must be checked separately before any staging merge.

## Remaining decisions

An owner must separately authorize hosted isolation/settings changes and the exact 48-filter deletion after reviewing a fresh target-bound snapshot. The 55 material-effect proofs need independently collected evidence and human signatures before a separately approved history repair; immediate history verification must follow it. The 13 forward migrations have standing authorization only after a fresh exact dry run, reviewed execution admission and strict post-apply proof. Main merge, production deployment and reopening remain distinct decisions. The production pause is maintained throughout.
