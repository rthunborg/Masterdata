# IPv4-only production isolation preparation — 2026-09-24

The reviewed PR #117 head `1cc93283f6c0f5d2c75a4270905b22c6aa54c052` merged to staging `d29a94a60ebdccff024c3f714874568ed96945eb` with an identical tree. Main remained `822350986f4c023948a7bbf490ddffc371185c4a`. The new isolation-gate implementation is `007f889e2ad637c609a2024853b5b7ee4ed93375` on a branch from that staging merge.

The owner confirms that the locally restore-tested backup is newer than the last production write and separately approves proceeding with temporary isolation work. This is an owner attestation; no backup contents or restore record were accessed. Two independent public IPv4 egress lookups agreed on the operator computer, while two IPv6 attempts found no usable route. The IPv4 candidate is saved with Windows DPAPI in private local inputs. The actual egress and private rollback state must be rechecked immediately before an isolation change.

Fresh source-bound post-merge staging proofs passed migration history 68/68, strict catalog 16/16, scoped advisors, and repayment/permission baselines. The original exact staging audit-baseline phase failed after an additional application audit row. A bounded read-only partition proof showed all 353 rows before the prior cutoff unchanged and a single later row, with all 354 rows mapped. The frozen phase is not relabeled as passed.

Fresh production read-only proofs still find no migration-history table, all 48 saved filters orphaned, 1,025 audit rows, and 61 permission-baseline rows. The strict production pre-apply catalog still fails the same seven named groups (9/16). Realtime is enabled, the Data API exposes both public schemas, database network restrictions include unrestricted IPv4 and IPv6, and existing clients are not drained. A broader writer inventory recognizes the pooler client, but one managed-profile login role and two preloaded worker backends still require complete live classification. The worker probe found no installed cron/net extension or usual job/request table in the connected database at that moment. These facts are a no-go, not isolation evidence.

The implementation adds an exact second network-receipt shape for an operator with IPv4 access and no IPv6 egress. It requires a single operator IPv4 allowance, an empty IPv6 allowlist, no unrestricted ingress, and independent non-operator denial probes for both IP families. The prior exact dual-stack profile remains. All other pause, Edge Function, Auth hook, Realtime, Data API, database-role, and session-drain checks remain strict; a successful assessor result remains evidence only and grants no production-write authority.

Focused isolation-gate Vitest passed **20/20**, with negative cases for each added requirement. TypeScript passed. Full ESLint exited 0 with **zero errors and 296 pre-existing warnings**. Exact `npx vitest run` on the guarded local fixture passed **4,000**, with **47 classified skips**, zero failures, and exit 0; report duration **174.16 seconds**; log SHA-256 `25dcaf87a21ebd79a747742ed4aa922c9dd1ee09b5610243c27b6b578861e1d8`. The fixture used a clean 68-migration chain plus seed, strict catalog 16/16, and a local-only 68-version history table. Earlier full attempts are retained as failures in local support: the first had two catalog failures against an unrelated drifted local database; a new managed fixture initially lacked local history; a later run had one protected-runner test timeout under load. That test passed by itself and the final exact full rerun passed. No failed result was counted as a pass.

Exact `npx playwright test` passed **163**, with **47 skipped**, zero failures/errors, and exit 0; the JUnit report records **210 total** and **1,118.336882 seconds**, and the guard-contained runner took **1,128.286 seconds**. All 47 skipped testcase identities exactly match the earlier PR #117 run; no new skip appeared. Browser log SHA-256: `c2f20232ba17b57928cf0fcc66e95d3b6eec71ef36db613daddac43ea8c37781`. The named local staging-preview build exited 0 (log SHA-256 `0e7bcb7432e580d74fd0e673afb662030cb7e7a05c5bc1998ed25786a8ddbf2d`). The guarded test resources received Stop requests; no local volume was removed.

At this local-gate snapshot, GitHub/Vercel checks and external review were pending; they must be checked against the eventual final PR head. No hosted setting, database row, migration history, deployment, alias, or production pause changed. Story 22.15 remains in-progress, Epic 23 on hold, and production paused/no-go. Full five-plane isolation, separately approved filter cleanup and history repair, 13 forward executes, main merge, deployment, and reopening remain gated.
