# Supabase Migrations-Only Change Policy

> **Current execution record — 2026-09-10 15:14 UTC.** PR #98 approved head ac38f8e874b61948809c5dfdb09ca2df054da254 merged to staging 62a52e32aae8302d6c6be4b35ec39da298c5061c with an identical tree. Authorized correction 20260910115024 applied at 2026-09-10T15:12:39.328Z; immediate history was 66/66 with no pending or remote-only versions. Strict post_apply catalog passes 15/15, security advisors report 0 WARN+ within pinned CLI coverage, performance retains only 3 classified multiple_permissive_policies WARN, and repayment aggregates/all four permission hashes are unchanged. Hosted direct-role/RPC acceptance and owner staging verification remain open. Production requires fresh inventory, signed history-proof ledger, backup and separately authorized history repair/isolation/settings/deployment; the owner has supplied standing authorization for required future migration applies, subject to reviewed prerequisites. Production remains paused; reopening and staging/main merges require separate authorization. Story 22.15 remains in-progress; Epic 23 on hold. [Completed reconciliation evidence](evidence/staging-reconciliation-completed-2026-09-10.md). This record supersedes earlier statements that PR #98 review, its merge, the correction apply or migration-apply authorization are pending; earlier dated entries remain historical.


Status: active from Story 22.10 (2026-06-14), hardened by Story 22.15 through the 2026-09-10 66-version post-apply ACL/policy-initplan correction

Staging is reconciled at candidate `62a52e32aae8302d6c6be4b35ec39da298c5061c`: correction `20260910115024` applied successfully on 2026-09-10, history is 66/66 with zero pending/remote-only versions, strict post_apply passes 15/15, both initplan warnings are removed, and repayment aggregates/all four permission hashes are unchanged. Complete hosted active/inactive fixture/RPC acceptance and owner staging verification. Production remains subject to fresh inventory, signed repair ledger, backup and separately authorized history repair/isolation/settings/deployment. Standing owner authorization covers required future migration applies; merges and reopening remain separate. Production stays paused, Story 22.15 in-progress, Epic 23 on hold. [Completed staging evidence](evidence/staging-reconciliation-completed-2026-09-10.md).

## Policy

All hosted Supabase **schema, RLS policy, function, grant, and trigger** changes — on **staging and production** — go through version-controlled migrations in `supabase/migrations/` applied with the Supabase CLI (or the equivalent reviewed migration apply). **No dashboard / SQL-editor schema or policy edits.**

This closes the drift that Story 22.8 found (production had 26 RLS policies vs 22 in migrations, an empty remote migration history, and `search_path`/grant advisory gaps — all from out-of-band dashboard edits; see `R-010`, `R-020`, `R-023`).

## Rules

1. **Author forward-only migrations.** Every hosted schema/policy/function/grant change is a new timestamped file in `supabase/migrations/` (`YYYYMMDDHHMMSS_<desc>.sql`), idempotent/guarded where practical (`DROP POLICY IF EXISTS`, `CREATE OR REPLACE FUNCTION`, `to_regprocedure` guards, `ADD/DROP COLUMN IF EXISTS`). Never edit, rename, or delete a migration version that may have been applied or materially represented on a hosted database.
2. **Verify locally first.** Run the full clean local migration chain only on a guard-owned disposable fixture, then run the RLS evidence suite (`tests/integration/epic-22/story-22.7/supabase-rls-evidence.test.ts`) and reconciliation evidence suite (`tests/integration/epic-22/story-22.10/`) before any hosted apply. Do not reset, start, or adopt a user-owned local stack.
3. **Prove before repairing history.** `supabase/migration-baseline-manifest.json` classifies every repository version exactly once. A version may be repaired as applied only when the machine fail-closed read-only catalog wrapper and environment-specific evidence prove its material state. The explicit reviewed connection mode, CLI link, and separately supplied intended-project reference must all match without being logged. Direct mode additionally binds the project-specific hostname; IPv4 Shared Supavisor session-pooler mode additionally binds the project reference in `postgres.<project-ref>`, the separately approved exact pooler hostname, port `5432`, and `verify-full`. Transaction pooling on `6543` remains prohibited. Any mismatch stops the cutover and requires an approved forward reconciliation migration.
4. **No wildcard repair.** Repair only the explicit reviewed version list for the selected environment, and repeat target binding immediately before each repair command. The wrapper requires `--reviewed-environment staging|production` to match `EXPECTED_SUPABASE_ENVIRONMENT` from the reviewed private target record and accepts only that environment's manifest `repair-after-catalog-proof` set. A newly added or forward-only migration must never be swept into or manually supplied to a historical repair. Unsafe historical versions, including repayment conversion, dietary-permission replacement, staffing creation/seed, and `user_filters` creation, must not be replayed over represented state.
5. **Dry-run the exact forward set.** After the approved repair list, run `node supabase/verify/run-reviewed-supabase-cli.mjs db push --reviewed-target --dry-run --skip-vault` from an immutable reviewed commit. The wrapper must re-prove the target and reviewed CA, reject leading or embedded root options, reject every native CLI target/password selector including `-p`, require an exact runbook command shape, and bind the CLI's actual connection to the validated settings. The output must contain only the manifest-approved executable list; any extra/missing version stops the operation.
6. **Keep the remote history in sync.** After apply, `node supabase/verify/run-reviewed-supabase-cli.mjs migration list --reviewed-target` must show local↔remote in sync for staging and production. Never push net-new schema against an environment whose remote history is not baselined.
7. **Staging before production.** Apply and verify on staging, then production — never the reverse. The owner authorized all required future migration applies on 2026-09-10; record that standing authorization against the exact reviewed production candidate, ordered files and SQL hashes once the fresh backup, owner-approved maintenance window, catalog/history proof and separately authorized isolation prerequisites pass (see `27_supabase_cutover_runbook.md`). Migration apply does not authorize history repair, hosted settings, deployment or reopening; the production pause remains intact.
8. **No dashboard schema edits.** Auth/platform settings that are genuinely dashboard-only (e.g. leaked-password protection, session/MFA config) are recorded as dated "changed on `<date>` by `<operator role>`" entries in the evidence index, since they are not expressible as SQL migrations.
9. **Local-stack parity is not hosted schema.** `supabase/seed.sql` grants the public-schema API-role table privileges that the hosted platform provisions automatically but the local CLI stack does not. It runs only on `supabase db reset`/`start` and is never applied to hosted environments — it is local parity, not a hosted change.

## Why

- A migrations-built environment must reproduce intended production behavior (rebuild, staging, disaster recovery). Out-of-band edits make the canonical source (`supabase/migrations/`) untrustworthy and were the root cause of the Story 22.8 drift findings.
- Reviewable, version-controlled change history is required for the commercial-readiness security posture (`08_security_overview.md`, `22_supabase_security_evidence_package.md`).

## See also

- `docs/commercial-readiness/27_supabase_cutover_runbook.md` — the staging baseline + production cutover runbook.
- `docs/commercial-readiness/26_environment_reconciliation_inventory.md` — the Story 22.10 drift inventory and reconciled end state.
- `docs/commercial-readiness/22_supabase_security_evidence_package.md` — migration-history, RLS-drift, and advisor evidence.

## Current post-apply application of the policy — 2026-09-10

Staging is reconciled at candidate `62a52e32aae8302d6c6be4b35ec39da298c5061c`: correction `20260910115024` applied successfully on 2026-09-10, history is 66/66 with zero pending/remote-only versions, strict post_apply passes 15/15, both initplan warnings are removed, and repayment aggregates/all four permission hashes are unchanged. Complete hosted active/inactive fixture/RPC acceptance and owner staging verification. Production remains subject to fresh inventory, signed repair ledger, backup and separately authorized history repair/isolation/settings/deployment. Standing owner authorization covers required future migration applies; merges and reopening remain separate. Production stays paused, Story 22.15 in-progress, Epic 23 on hold. [Completed staging evidence](evidence/staging-reconciliation-completed-2026-09-10.md).


### Historical pre-correction rationale (superseded)

Owner-approved PR #97 reached staging history 65/65 through one separately authorized repair and seven separately authorized forward applies. Strict post-apply catalog proof found only extra `service_role` EXECUTE on `get_user_role()`. The response is new forward-only `20260910115024_reconcile_post_apply_acl_and_policy_initplans.sql`, which revokes that grant and changes caller identity predicates in two policies: `column_config` `USING` and `WITH CHECK`, plus `employee_column_changes` `USING` (three expressions total); it does not write application data. Current staging repair set is empty and execute set contains only this version. Historical pre-review implementation verification on `eceaee7fa423c87aa9e6e235a70e7bb4eceb746d` passed: Vitest 3,451/3,451 with zero skips across 322 files (exit 0; 571.55s report / 572.9867885s wall; 12:20:00.7652475Z–12:29:33.7495182Z) and `npx playwright test` 163 passed / 47 skipped / 0 failed / 0 errors (exit 0; 1326.864669s report / 1339.0507151s wall; 12:30:22.4517860Z–12:52:41.5005005Z; XML SHA-256 `484732953160ce5abef9b8819aae0ceee4b5c716b322f3588920607d581fe11d`). The skip identities exactly matched PR #96: 9 notification/cron authorization cases and 38 fixture/superseded debt cases. The code-identical `e122197f1ff33b560a2429902c8ca46cc33b33c8` clean 66-migration-plus-seed chain passed 15/15 in 9.781s; TypeScript, final-fixture lint (0 errors; prior full lint 0 errors/297 warnings), staging preview build exit 0 in 16.3758345s, expected production refusal exit 1, and required ÖMC PostgREST 1/1 in 2.11s also passed. PR #98 head `56b4fb2` Reviewbot identified P1: the complete policy proof omitted table RLS-enabled state; and P2: the ACL proof omitted non-owner `EXECUTE WITH GRANT OPTION`. Both findings are fixed in verifier/regression code on exact `2c8f0066bb4eda847f2ed6ae294a56dd87125652`; the forward migration is unchanged. Fresh `npx vitest run` passed 3,451/3,451 with zero skips across 322 files (exit 0; 539.44s report / 540.8180165s wall; 13:16:45.4504105Z–13:25:46.2662654Z). Required ÖMC PostgREST passed 1/1 (exit 0; 2.51s); focused verification passed 83/83 (3.92s); the clean 66-migration-plus-seed chain passed 15/15 in 9.064s with cleanup verified; catalog SHA-256 is `f35dd0d9a58e2b87dfb51387d301763c8a39d39f71af72b5e557765c7d10014e`; TypeScript and lint passed with 0 errors/297 warnings. Reviewbot completed clean on 2c8 at 13:22:56Z. Exact `npx playwright test` passed on 2c8: exit 0; 163 passed / 47 skipped / 0 failed / 0 errors (210 total; `skipListMatchesReviewedPr96=true`; the same 9 notification/cron-authorization and 38 fixture/superseded-debt classifications); 1194.999401s report / 1205.8919197s wall; 13:26:27.7930473Z–13:46:33.6829666Z; XML SHA-256 `5a37a267454a33d40d9a9edf9b4a232fd3d39e487970f65c044e5bbe8c282477`. GitHub/Vercel are green on 2c8; CI run `34481745929` passed unit 3,378 with 73 environment-managed skips across 311 passing/11 skipped files (486.97s) and integration 827 with 73 environment-managed skips across 93 passing/11 skipped files (149.57s), distinct from local Vitest zero skips. Reviewbot is clean. Final test-only selector `269e3d5634c68e1bc502839678247833e1cccc54` changes only Vitest test code: it waits for FilterPanel initial focus, then asserts the named Save Dialog exact full input value and the same POST/toast. Full Vitest passed 3,451/3,451 with zero skips across 322 files (exit 0; 475.11s report / 476.3469515s wall; 14:09:48.5316236Z–14:17:44.8762464Z); focused lead evidence passed 7/7 in 5.01s and three agent runs passed 7/7. TypeScript passed; full lint passed with 0 errors/296 warnings. Application, migration, verifier, and Playwright sources are identical to 2c8, so the exact 163/47/0/0 Playwright result remains valid 2c8-scope evidence and was not rerun on 269. The 2c8 clean 66-plus-seed chain remains valid because the SQL is unchanged. The final documentation-only head still needs its own CI, Vercel, and Reviewbot verification after push before merge approval. No further hosted write is authorized; read-only observation remains allowed. Production remains independently fresh-inventory and owner-gated at 57 repairs plus nine executes, and the production pause remains active.
