# Task Brief: Dev Agent — Implement Story 21.8

## Your Role
You are a Fullstack Developer agent. Implement Story 21.8 — Email Notification on Staffing Need Update.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.8.md` — Your story spec (read FIRST)
2. `src/lib/services/email-service.ts` — Existing email service to follow pattern
3. `src/lib/services/omc-masterdata-reminder.ts` — Reference for notification pattern
4. `src/lib/services/pe3-deadline-notifications.ts` — Another reference
5. `src/app/api/staffing-needs/route.ts` — PUT handler where notification is triggered
6. `src/lib/server/repositories/user-repository.ts` — For querying HR users
7. Only if needed: `docs/architecture.md`

## What to Implement

### 1. Notification service
Create: `src/lib/services/staffing-needs-notification.ts`
- Function to send email to HR users (hr_admin + recruiter roles) when staffing need is updated
- Exclude the editor (user who made the change)
- Fire-and-forget pattern (don't await, log errors)

### 2. Wire into API
Update: `src/app/api/staffing-needs/route.ts` (PUT handler)
- After successful update, fire the notification (don't await)

### 3. Tests
Create: `tests/unit/epic-21/story-21.8/` with test files covering:
- Email sent to correct recipients (hr_admin + recruiter, excluding editor)
- Email not sent when value unchanged
- Errors are caught and logged, not thrown

## Constraints
- Fire-and-forget: don't await the notification, just .catch(console.error)
- Follow existing email service patterns
- Swedish email content
