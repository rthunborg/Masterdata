# Task Brief: Dev Agent — Implement Story 21.6

## Your Role
You are a Fullstack Developer agent. Implement Story 21.6 — Edit Staffing Needs Modal.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.6.md` — Your story spec (read FIRST)
2. `src/components/dashboard/staffing-needs-tracker.tsx` — Parent component that opens this modal
3. `src/components/dashboard/staffing-needs-card.tsx` — Card with pencil icon trigger
4. `src/components/dashboard/edit-employee-modal.tsx` — Reference for existing modal + RHF + Zod pattern
5. `src/lib/validation/staffing-needs.ts` — Zod schema
6. `src/lib/types/staffing-needs.ts` — Types
7. Only if needed: `docs/architecture.md`

## What to Implement

### 1. New component
Create: `src/components/dashboard/edit-staffing-needs-modal.tsx`
- Modal with React Hook Form + Zod resolver
- Two number inputs (one per location), pre-filled with current values
- Title: "Uppdatera bemanningsbehov"
- Save: PUT /api/staffing-needs for each changed location
- Success toast, card refresh (invalidate query), close modal
- Cancel closes without saving

### 2. Update existing
- `src/components/dashboard/staffing-needs-tracker.tsx` or `staffing-needs-card.tsx` — Wire up pencil icon click to open the edit modal

### 3. Tests
Create: `tests/unit/epic-21/story-21.6/` with test files covering:
- Modal renders with pre-filled values
- Submitting calls PUT for changed locations only
- Cancel closes without API calls
- Validation rejects negative numbers

## Constraints
- WCAG 2.1 AA contrast
- Swedish copy for all labels, buttons, toast messages
- React Hook Form + Zod for validation
- Use existing Dialog/Modal patterns from shadcn/ui
- Use TanStack Query mutation for PUT + query invalidation
