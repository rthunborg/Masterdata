# Task Brief: Dev Agent — Implement Story 21.4

## Your Role
You are a Fullstack Developer agent. Implement Story 21.4 for the hr-masterdata project.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.4.md` — Your story spec (read FIRST)
2. `src/lib/utils/role-utils.ts` — You need to ADD a function here
3. `src/lib/types/employee.ts` or `src/lib/types/index.ts` — Reference for existing type patterns
4. Only if needed: `docs/architecture.md`

## What to Implement

### 1. Types file
Create: `src/lib/types/staffing-needs.ts`
- Export STAFFING_LOCATIONS const array, StaffingLocation type
- Export StaffingNeed, StaffingNeedWithProgress, StaffingNeedsChangelogEntry interfaces
- Follow exact schema from story file

### 2. Validation file
Create: `src/lib/validation/staffing-needs.ts`
- Export updateStaffingNeedSchema Zod schema
- Export UpdateStaffingNeedInput type

### 3. Role utility
Update: `src/lib/utils/role-utils.ts`
- Add canEditStaffingNeeds(role) function — returns true for hr_admin and crewing
- Follow existing patterns in that file

### 4. Tests
Create: `tests/unit/epic-21/story-21.4/` with test files for:
- Zod schema validation (valid input, invalid location, negative headcount, non-integer)
- canEditStaffingNeeds role checks (allowed and denied roles)

## Constraints
- Supabase RLS enforcement (types match DB)
- Zod schema validation
- Follow existing patterns in the codebase
- Check existing type files for naming conventions
