# Task Brief: Dev Agent — Implement Story 21.3

## Your Role
You are a Fullstack Developer agent. Implement Story 21.3 for the hr-masterdata project.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.3.md` — Your story spec (read FIRST)
2. `src/app/api/employees/route.ts` — Reference for existing API route pattern
3. `src/lib/server/api-helpers.ts` — For requireAuth, response helpers
4. `src/lib/server/auth.ts` — For auth middleware
5. `src/lib/server/repositories/staffing-needs-repository.ts` — Repository from 21.2
6. `src/lib/validation/staffing-needs.ts` — Zod schema from 21.4
7. `src/lib/types/staffing-needs.ts` — Types from 21.4
8. Only if needed: `docs/architecture.md`

## What to Implement

### 1. API Routes
Create these Next.js API route files:
- `src/app/api/staffing-needs/route.ts` — GET (getAll) and PUT (updateNeed) handlers
- `src/app/api/staffing-needs/history/route.ts` — GET handler with ?location query param

### 2. Tests
Create: `tests/integration/epic-21/story-21.3/` with test files covering:
- GET /api/staffing-needs returns correct shape
- PUT /api/staffing-needs with valid data succeeds for hr_admin/crewing
- PUT /api/staffing-needs returns 403 for unauthorized roles
- PUT /api/staffing-needs returns 400 for invalid body
- GET /api/staffing-needs/history returns entries for valid location
- GET /api/staffing-needs/history returns 400 for invalid location

Follow existing test patterns — mock the repository and auth.

## Constraints
- Use requireAuth from api-helpers for auth checks
- Use canEditStaffingNeeds from role-utils for role checks on PUT
- Validate PUT body with updateStaffingNeedSchema
- Email notification on PUT is fire-and-forget (don't await, just call and catch errors)
- Response format: { data: ..., meta: { timestamp: new Date().toISOString() } }
- Follow existing API route patterns exactly
