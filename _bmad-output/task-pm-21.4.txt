# Task Brief: PM Agent — Draft Story 21.4

## Your Role
You are a Product Manager agent. Draft a complete story file for Story 21.4.

## Context Files to Read (ONLY these)
1. `docs/epics.md` — Search for "Story 21.4" to find the epic-level spec
2. `docs/architecture.md` — Only if you need structural patterns
3. `docs/sprint-artifacts/story-21.1.md` — As a FORMAT REFERENCE for how story files should look

## What to Produce

Create file: `docs/sprint-artifacts/story-21.4.md`

Story 21.4: Types & Validation Schemas

From the epics doc, the story covers:
- TypeScript interfaces: `StaffingNeed`, `StaffingNeedWithProgress`, `StaffingNeedsChangelogEntry`
- Zod schema: `updateStaffingNeedSchema` for PUT request validation
- Role utility: `canEditStaffingNeeds(role)` → true for hr_admin and crewing
- Files: `src/lib/types/staffing-needs.ts`, `src/lib/validation/staffing-needs.ts`, and update to `src/lib/utils/role-utils.ts`

## Story File Structure (follow story-21.1.md format)
1. Status block (P1, 1 SP, no dependencies)
2. Description (user story format)
3. Acceptance Criteria (checkboxes, detailed)
4. Technical Notes (exact file paths, interfaces, schemas)
5. Testing Requirements (estimated tests: 0 per sprint status — these are type/schema files, but if you see value in unit tests for Zod schemas, note it)
6. Definition of Done

## Constraints
- Types must match the DB schema from Story 21.1
- `staffing_needs` table: id, location, headcount_need, updated_at, updated_by
- `staffing_needs_changelog`: id, location, old_value, new_value, changed_by, changed_at
- `StaffingNeedWithProgress` should include computed fields for crew-ready count and percentage
- Zod schema validates: location enum ('Trelleborg'|'Göteborg'), headcount_need (integer >= 0)
