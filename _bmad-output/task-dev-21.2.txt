# Task Brief: Dev Agent — Implement Story 21.2

## Your Role
You are a Fullstack Developer agent. Implement Story 21.2 for the hr-masterdata project.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.2.md` — Your story spec (read FIRST)
2. `src/lib/server/repositories/employee-repository.ts` — Reference for existing repository pattern
3. `src/lib/types/staffing-needs.ts` — Types from Story 21.4
4. `src/lib/supabase/server.ts` — For createClient usage
5. `src/lib/services/notification-helpers.ts` — For getTodayStockholm()
6. Only if needed: `docs/architecture.md`

## What to Implement

### 1. Repository file
Create: `src/lib/server/repositories/staffing-needs-repository.ts`

Methods:
- `getAll()` — Returns both locations with headcount_need, done_count (employees WHERE town_district=location AND crewing_done=true AND is_archived=false), updated_at, updated_by email, last_change (latest changelog entry per location)
- `updateNeed(location, newValue, userId)` — Fetch current, update, insert changelog, return {oldValue, newValue}. Skip if same value.
- `getHistory(location)` — Changelog for current year (Stockholm tz), DESC order, with changed_by email

Export as singleton.

### 2. Tests
Create: `tests/integration/epic-21/story-21.2/` with test files covering:
- getAll returns correct shape with done_count
- updateNeed creates changelog entry and skips when value unchanged
- getHistory returns current-year entries in DESC order

Follow existing test patterns in `tests/integration/` — mock Supabase client.

## Constraints
- Use types from `src/lib/types/staffing-needs.ts`
- Follow existing repository singleton pattern
- Supabase RLS enforcement (repository runs through RLS)
- Use getTodayStockholm() for year boundary in getHistory
