# Task Brief: Dev Agent — Implement Story 21.1

## Your Role
You are a Fullstack Developer agent. Implement the database migration for Story 21.1.

## Context Files to Read (ONLY these)
1. `docs/sprint-artifacts/story-21.1.md` — Your story spec (read this FIRST, it has everything)
2. Only if needed: `docs/architecture.md` for structural unknowns

## What to Implement

### 1. Migration File
Create: `supabase/migrations/20260313000001_add_staffing_needs.sql`

Look at an existing migration for the pattern (e.g., `supabase/migrations/20251109160000_add_capacity_columns.sql` or similar). Wrap in BEGIN/COMMIT.

The migration must:
- Create `staffing_needs` table with exact schema from story spec
- Create `staffing_needs_changelog` table with exact schema from story spec
- Enable RLS on both tables
- Create 4 RLS policies (2 per table) using `get_user_role()`
- Create index on changelog (location, changed_at DESC)
- Seed 2 rows into staffing_needs (Trelleborg, Göteborg)
- Update column_config to revoke crewing edit on crewing_done

### 2. Tests
Create directory: `tests/integration/epic-21/story-21.1/`

Write 2 test files:
1. `migration-integrity.test.ts` — tables exist, columns correct, constraints valid, index exists, RLS enabled
2. `rls-seed-data.test.ts` — seed rows present (Trelleborg & Göteborg with headcount_need=0), SELECT/UPDATE permissions by role, INSERT into changelog restricted, crewing_done permission updated

Follow existing test patterns. Check `tests/integration/constraints/` for examples of schema/RLS testing. Check `tests/setup.ts` and `tests/mocks/supabase.ts` for test infrastructure.

## Constraints
- Supabase RLS enforcement
- Follow existing migration patterns in the project
- No frontend code needed
- No i18n needed (backend only)

## When Done
List all files created/modified.

============================================================
CRITICAL SYSTEM INSTRUCTION:
When you have 100% completed your entire task, output the
done marker by combining these two halves with no space:
  First half:  [BMAD_TASK_
  Second half: COMPLETE]
The result should be one token on its own line. Do not ask
for further instructions after outputting this marker.
